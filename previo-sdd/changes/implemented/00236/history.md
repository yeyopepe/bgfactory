# Prompt history — 00236

Historical information about the analysis process, not current information. Records, verbatim and without rephrasing, the successive prompts with which the user raised and expanded this entry — they can be incomplete or contradictory with each other, since they reflect how the request evolved session by session, not the final result (that lives in `description.md`).

**Exclusive use of `pv-new` and `pv-fix`.** No other skill in the framework (`pv-how`, `pv-do`, `pv-status`, etc.) should read this file or take it into account: the source of truth for what's being asked is always `description.md`.

## 2026-09-03 — initial session

nuevo tipo de elemento llamado "Baraja francesa estándar (54 cartas)":
- 52 cartas con la numeración y colores de los 4 palos
- 2 cartas de Joker
- Cada carta está diseñada con el símbolo de su palo en el centro de la carta (Crea las imágenes SVG de las cartas (estilo simple) como recursos ), el número en las esquinas superior izquierda e inferior derecha y todo con el color correcto de cada palo y fondo blanco
- el reverso de las cartas que seas un patrón geométrico sencillo en blanco y azul (también creado como recurso)
- Al crearlas las muestra en la mesa desplegadas y ordenadas en 5 filas (los 4 palos y los Joker)
- Crea un grupo que las agrupe todas después de crearlas, para que sea más fácil seleccionarlas por el usuario

En el menú con las listas de componentes inventa las cartas/fichas y esta baraja con el título "Cartas/Fichas"
