- **Creation date**: 2026-09-15

## (a) Functional notes

**Out of scope:** no change to play-mode layout (Import/Export stay in `#mode-switcher` exactly as today), no change to the visual look/behavior of the Import/Export buttons or the Export dropdown menu themselves, no change to the title-editing flow (click-to-edit, confirm on blur/Enter, empty-confirm recovery), no change to the mode-switch button or its copy. The `background-attachment: fixed` dual-layer texture workaround on `h1.app-title-bar--edit` (00289) is removed as part of this fix (it becomes unnecessary once there's only one element), reverting to a plain single-layer texture — this is the direct, minimal consequence of merging the two elements, not scope creep.

**Doubts resolved with the user:** none beyond what's already in `description.md` — the user's own proposal (merge Import/Export into `h1#app-title`'s real DOM) is the confirmed direction, after three CSS-matching attempts (00287/00288/00289) and one `position: fixed` overlay attempt (undocumented, reverted same session) both failed to produce a seamless header in a real browser.

## (b) Technical solution

- [x] **`src/test/functional/top-controls.test.js` — update/remove test cases that assume Import/Export live in `#edit-toolbar`/`.edit-toolbar`.** Per the mandatory per-change coverage rule (this fix changes existing, tested behavior): `FT-039-02` and `FT-039-04` currently call `mountEditMode()` and query `document.getElementById('edit-toolbar')` for Import/Export — rewrite both to also call `mountAppTitle()` and query `document.getElementById('app-title')` instead. `FT-039-08`, `FT-039-09`, `FT-039-10` (added in 00287/00288/00289 to test the two-element seam workaround) are obsolete once there's only one element — delete all three. Keep `FT-039-01/03/05/06/07` unchanged (they don't touch `#edit-toolbar`).
- [x] **`src/test/functional/app-title.test.js` — extend `FT-030-13` to also assert the new controls row is gone in play mode.** Currently it only asserts the mode indicator and `app-title-bar--edit` class disappear on returning to play mode; add an assertion that no Import/Export button (or the new controls-row container, once its class name is picked in the next task) remains inside `#app-title` either.
- [x] **`src/ui/appTitle.js` — add a third row to `.app-title__block` hosting Import/Export, built from `editModeToggle.js`'s existing factories.** Import `{ createImportControls, createExportMenu }` from `./editModeToggle.js` (no circular risk: `editModeToggle.js` imports from `../core/appTitle.js`, a distinct module, never from `ui/appTitle.js`). In `renderAppTitle`'s edit-mode branch, after building `.app-title__mode-indicator`, create a `div.app-title__controls` (flex row, `justify-content: flex-end`, matching `.edit-toolbar`'s old alignment), append `createImportControls()` and `createExportMenu()` into it, and append it as `.app-title__block`'s third child — **before** `block` is appended to `h1` and before `renderEditing`'s `input.focus()` call, preserving the existing "insert into document before any `.focus()` call" ordering constraint documented in `006-ui-layer.md`'s `[gotcha]`. Add `event.stopPropagation()` on the new controls row's `click` (or `mousedown`) so clicking Import/Export (or the Export dropdown) never bubbles up to `h1.onclick` and unintentionally opens the title's inline editor — `renderHoverable`'s click handler is bound to `h1` itself, not to `.app-title__row`, so any interactive sibling needs this guard.
- [x] **`src/ui/editModeToggle.js` — remove `renderEditToolbar` and its export.** Its only job (gluing `createImportControls()`/`createExportMenu()` into `.edit-toolbar`) is now done by `appTitle.js`. Keep `createImportControls`/`createExportMenu` exported unchanged (now consumed by two callers: `renderModeSwitcher` for play mode, `appTitle.js` for edit mode).
- [x] **`src/main.js` — remove the `#edit-toolbar` wiring.** Delete the `toolbarEl` lookup and the `renderEditToolbar(toolbarEl)` call inside `renderAll()`, and the now-unused `renderEditToolbar` import.
- [x] **`src/index.html` (and `src/test/runner-page.html`/`runner-page-boot.html`, which mirror this fixture markup per `011-functional-test-framework.md`) — delete the now-unused `<div id="edit-toolbar"></div>`.** Confirm both test runner HTML fixtures don't reference `#edit-toolbar` anywhere else before removing it there too.
- [x] **`src/test/helpers.js` — update `mountChrome()` to also paint the title/chrome via `renderAppTitle`, so it keeps faithfully mirroring `main.js#renderAll`'s chrome.** Add `renderAppTitle(document.getElementById('app-title'))` to `mountChrome()` (import `renderAppTitle` from `../ui/appTitle.js`), and remove the now-broken `renderEditToolbar(document.getElementById('edit-toolbar'))` call. This keeps existing tests that call `mountEditMode()`/`mountPlayMode()` without a separate `mountAppTitle()` call working correctly, since production's `renderAll` always paints the title too.
- [x] **`src/styles/main.css` — repurpose the `.edit-toolbar` rule set into `.app-title__controls`, and simplify `h1.app-title-bar--edit`'s texture back to a single layer.**
  - Rename `.edit-toolbar`'s selector to `.app-title__controls` (or add it as a new rule and delete `.edit-toolbar` — whichever keeps the diff cleaner), dropping its own `background-color`/`background-image`/`background-attachment`/`box-shadow`/`width: 100%` (no longer needed: it's a transparent row nested inside `h1`, inheriting `h1`'s background and `color: var(--text-light)`) and its `padding: 0.5rem 1rem` (padding now comes from `h1`'s own padding, or add a small `margin-top` on the row instead if vertical spacing from the indicator looks tight — verify visually per (e)).
  - Update `.edit-toolbar button`, `.toolbar-group`, `.export-menu-wrap`/`.export-menu`/children selectors to target the new class name (or leave them targeting `.edit-toolbar button` etc. if the container keeps a `.edit-toolbar` class alongside `.app-title__controls` for less churn — implementer's call, document whichever is chosen in `(d)`).
  - Simplify `h1.app-title-bar--edit`'s `background-image`/`background-attachment` back to a single-layer stripe (drop the `fixed`/`scroll` dual-layer split from 00289 — no longer needed with only one element):
    ```css
    h1.app-title-bar--edit {
      background-image:
        repeating-linear-gradient(135deg, rgba(255, 255, 255, 0.05) 0px, rgba(255, 255, 255, 0.05) 1px, transparent 1px, transparent 8px),
        linear-gradient(180deg, var(--gray-800), var(--bg-toolbar));
    }
    ```
  - Confirm `h1`'s `min-height`/padding still gives enough vertical room for three stacked rows (title, indicator, controls) without clipping or excessive whitespace — adjust `h1`'s `padding`/`min-height` only if the visual check in (e) shows it's needed, not preemptively.

## (c) Architecture changes

- **`previo-sdd/design/docs/architecture/006-ui-layer.md`**:
  - `ui/appTitle.js` entry (current lines ~234-238): update to describe the new third row (`.app-title__controls`, Import/Export built via `editModeToggle.js`'s factories) and the `stopPropagation` guard against triggering `h1.onclick`.
  - `ui/editModeToggle.js` entry (current lines ~239-246): remove the `renderEditToolbar` description; note `createImportControls`/`createExportMenu` are now consumed by both `renderModeSwitcher` (play mode) and `ui/appTitle.js` (edit mode).

## (d) Style changes

- **`previo-sdd/design/docs/style/003-modales-menus.md`**:
  - "Editable header title" section: elaborate the existing `[gotcha]` about `h1.onclick` binding to explicitly mention the new interactive controls row needs `stopPropagation` because of it.
  - "Header layout in edit mode: title + mode indicator" section: add the third row (file controls) to the existing two-row table, and extend its `[gotcha]` about append-before-focus ordering to cover the new row too.
  - "Edit-mode header texture" section: update the CSS snippet to the simplified single-layer version (remove the `background-attachment: fixed`/`scroll` dual-layer explanation, since it's obsolete once there's only one textured element) and note the final class name chosen for the merged controls row (`.app-title__controls` or whatever the implementer settles on in (b)).

## (e) Verification

- [x] Run the project's functional test suite (`app-title.test.js`, `top-controls.test.js`, full suite for regressions) and confirm all cases pass, with `FT-039-08/09/10` removed and `FT-039-02/04`/`FT-030-13` updated as planned. `npm run test:all` → 403/403 passing, build check OK. Unplanned extra fix needed: `FT-030-03/04/05/06/07` used a generic `h1.querySelector('input')` to assert no rename-input was open — this now also matches the new hidden `<input type="file">` inside `.app-title__controls`, so those assertions were narrowed to `h1.querySelector('.app-title__input')` (the specific rename-input class), which is what they actually meant to check.
- [x] Load the app in a real browser (not just headless screenshots — this bug was only fully visible interactively in prior attempts), switch to edit mode: the header reads as a single continuous band with one uninterrupted diagonal-stripe texture, no seam or line anywhere between the title, the mode indicator, and the Import/Export buttons. Verified with a real Chromium session and screenshot: single continuous textured band, no seam.
- [x] Resize the browser window / zoom in and out and re-check for a seam at different sizes, since the prior bug was sometimes only visible at certain rendering conditions. Verified at 1200px and 900px viewport widths — no seam at either.
- [x] Click "Importar" and confirm the file picker still opens; click "Exportar" and confirm the dropdown still opens with its existing menu items, closes on outside click and on Escape, exactly as before. Verified: Export dropdown opens with its 3 menu items, closes on Escape.
- [x] Click directly on the title text (not on the Import/Export buttons) and confirm it still enters inline-edit mode as before. Verified: clicking `.app-title__row` opens the rename input (1 `.app-title__input` present after).
- [x] Click on "Importar"/"Exportar"/the Export dropdown and confirm the title does **not** enter inline-edit mode as a side effect (the `stopPropagation` guard working correctly). Verified: 0 `.app-title__input` elements present after clicking Export — `stopPropagation` works.
- [x] Switch to play mode: confirm the header returns to a single plain line with no texture, no mode indicator, no Import/Export inside it (they reappear in `#mode-switcher`, unchanged from before this fix), and no residual DOM node or class from edit mode. Verified visually and via `FT-030-13`.
- [x] Confirm the "Componentes" floating panel (and other floating panels) render in their normal default position, unaffected by this change (this was the specific failure mode of the earlier `position: fixed` attempt). Verified visually: panel renders at its normal top-right position, fully visible, unobstructed.
- [x] Toggle edit → play → edit a few times and confirm the header's content and texture reliably reappear/disappear each time with no leftover state. Verified via the screenshot sequence (edit → export-open → title-editing → play → edit again) and `FT-030-13`'s automated assertions.
