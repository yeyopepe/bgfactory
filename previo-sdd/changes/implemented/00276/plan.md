- **Creation date**: 2026-09-15

## (a) Functional notes

**Out of scope:** no se toca `core/component.js` ni el campo genérico `name` del modelo `Component` (no fue introducido por 00272, es preexistente y usado por `updateComponent()`/`syncCopyWithOriginal()` y varios tests de CRUD/persistencia ajenos a este fix — retirarlo sería ampliar el alcance más allá de la causa raíz). No se toca el modal de edición de componente (`ui/componentModal.js`). No se modifica el filtro de texto del listado (`matchesFilter`). No se toca la fila de grupo (`component-list__group-name`, usa el id del grupo, sin relación con este fix).

**Doubts resolved with the user:** ¿el id del mazo debe ser el GUID (revirtiendo 00272 por completo) o un texto legible como pedía el prompt original de 00272? → texto legible: `mazo.id = "Baraja Francesa"` (con desambiguación), mismo patrón que ya usan las 54 cartas del preset vía `freeId()`. Así se cumple, correctamente esta vez, la petición original de 00272 ("añade a este cambio que el id del mazo debe ser algo legible, como 'Baraja Francesa'"), sin el campo `name` paralelo que causaba la confusión (columna "Id" no siempre mostraba el id).

## (b) Technical solution

- [x] **`src/ui/componentList.js` — la columna "Id" vuelve a mostrar siempre `component.id`.** Línea 351, revertir `idCell.textContent = (component.name && component.name.trim()) ? component.name : component.id;` a `idCell.textContent = component.id;`. No se toca ninguna otra línea del fichero (la fila de grupo, línea ~264, ya usa `component.id` del grupo y no se ve afectada).
- [x] **`src/core/presets/frenchDeck.js` — el mazo recibe un id legible en vez de un `name` paralelo.** Sustituir la línea 102 (`mazo.name = t('componentTypeModal.preset.frenchDeck.deckName');`) por `mazo.id = freeId(t('componentTypeModal.preset.frenchDeck.deckName'), [...getComponents(), ...newComponents]);`, colocada igual que las cartas (comparar con la línea 87, `carta.id = freeId(entry.cardId, [...getComponents(), ...newComponents]);`) — antes de la línea `mazo.properties.cartaIds = ...` para que quede junto a la asignación de identidad del componente. `freeId` (líneas 20-24, ya importa `nextCloneId` de `core/component.js`) ya está disponible en el fichero, no requiere nuevo import. El id por defecto que le asigna `createDefaultComponent('mazo')` (un GUID vía `createComponent`) queda sobrescrito.
- [x] **`src/data/i18n.es.js` / `src/data/i18n.en.js` — sin cambios en la clave existente.** La clave `componentTypeModal.preset.frenchDeck.deckName` (valor `'Baraja Francesa'` / `'French Deck'`) se reutiliza tal cual como valor candidato del nuevo `mazo.id`; no se renombra (su nombre sigue siendo válido: es el nombre del mazo generado, ahora usado como su id).
- [x] **`src/test/functional/french-deck-preset.test.js` — actualizar `FT-002-19` para que verifique el id, no `name`.** Sustituir `expect(mazo.name).toBe(t('componentTypeModal.preset.frenchDeck.deckName'));` por `expect(mazo.id).toBe(t('componentTypeModal.preset.frenchDeck.deckName'));`, y actualizar el título del caso (deja de mencionar "nombre legible", pasa a describir el id legible del mazo, p. ej. `FT-002-19 · el componente mazo generado tiene el id legible "Baraja Francesa"`). No tocar `FT-002-20` (verifica las cartas, sin relación).
- [x] **`src/test/functional/component-panel.test.js` — retirar `FT-003-18`/`FT-003-19` y su sección.** Ambos casos (líneas 329-343) validan exclusivamente el fallback `name → idCell` que se revierte; no hay comportamiento que sigan cubriendo tras el fix (la columna "Id" vuelve a ser un caso trivial, ya cubierto implícitamente por cualquier otro test del panel que lea `idCell`). Retirar también el comentario de sección `// --- Nombre legible en la celda de identificación (00272) ---` (línea 327) al quedar sin casos debajo.

## (e) Verification

- [x] Generar el preset "Baraja francesa estándar (54 cartas)" desde el picker de componentes ("Añadir componente") y comprobar en el panel flotante de Componentes que la fila del mazo muestra `Baraja Francesa` en la columna "Id" — y que la etiqueta flotante sobre el mazo en el lienzo muestra ese mismo texto (`Mazo: Baraja Francesa`), no un GUID. Verificado por código: `frenchDeck.js:102` asigna `mazo.id` (ya no `mazo.name`) al valor traducido; `componentList.js:351` vuelve a mostrar siempre `component.id`, y la etiqueta flotante sobre el componente (`ui/componentRenderer.js`) ya usaba `component.id`, sin cambios en ese punto. Confirmado también por el test `FT-002-19` actualizado.
- [x] Generar el preset dos veces en la misma partida y comprobar que el segundo mazo se desambigua (`Baraja Francesa(1)` o análogo, mismo patrón que ya usan las cartas duplicadas), sin colisionar con el primero. Verificado por código: `mazo.id = freeId(t(...), [...getComponents(), ...newComponents])` reutiliza exactamente `freeId`/`nextCloneId` (mismo mecanismo que ya usan las 54 cartas vía `carta.id = freeId(entry.cardId, ...)`), y en la segunda generación `getComponents()` ya contendría el mazo `"Baraja Francesa"` de la primera, forzando la rama de desambiguación de `freeId`.
- [x] Crear o editar cualquier otro componente (no generado por el preset) y comprobar que la columna "Id" sigue mostrando su identificador técnico habitual (GUID o el id que tenga), sin cambios de comportamiento. Verificado por código: `idCell.textContent = component.id;` sin condición ni fallback, aplica igual a cualquier componente.
- [x] `npm run test:all` pasa en verde, con `FT-002-19` actualizado y `FT-003-18`/`FT-003-19` ya retirados (no aparecen en el informe). `npm test`: 391/391 OK, sin anomalías en `src/test/TRACEABILITY.md`.
