- **Creation date**: 2026-09-14

## (a) Functional notes

**Out of scope:** no se implementa modo oscuro (el sistema queda preparado, pero es trabajo futuro explícito); no se parte `main.css` en varios ficheros (se descarta explícitamente, ver tarea de reorganización); no se auditan exhaustivamente las ~3.702 líneas más allá de la lista cerrada de `design_data_migracion-valores-sueltos.md`; el tamaño `0.5em` de `.splash-window__title sup` se mantiene tal cual (relativo, no tokenizable); los tokens `--bg-table-dot` y `--section-accent` no entran en ninguna escala nueva, se mantienen intactos.

**Doubts resolved with the user:** ninguna duda técnica nueva planteada al usuario en esta fase — las tres decisiones que `description.md` dejaba abiertas para `pv-how` (opacidad suelta `rgba(44,125,216,0.2)`, formas de sombra distintas sobre el azul, y si se unifican `--transition-fast`/`--duration-fast`) se resuelven aquí mismo, en la sección (b), como parte del diseño técnico — no requerían confirmación del usuario porque se derivan directamente de los criterios ya fijados en `description.md` (evitar tokens de un solo uso, mantener la distinción funcional foco-normal/foco-reforzado).

## (b) Technical solution

- [x] **`src/styles/main.css` — Ampliar el bloque `:root` con todos los tokens nuevos.** Añadir, tras las 21 variables existentes (sin tocar su orden ni sus valores), los siguientes grupos de tokens nuevos, agrupados con un comentario de una línea por grupo:
  - Superficies: `--bg-surface: #ffffff;`, `--bg-overlay: rgba(0, 0, 0, 0.5);`
  - Acento azul (alphas): `--accent-blue-alpha-15: rgba(44, 125, 216, 0.15);`, `--accent-blue-alpha-25: rgba(44, 125, 216, 0.25);`, `--accent-blue-alpha-35: rgba(44, 125, 216, 0.35);`
  - Semánticos: `--error-subtle: rgba(211, 47, 47, 0.08);`, `--error-alpha: rgba(211, 47, 47, 0.35);`, `--success-subtle: rgba(46, 125, 50, 0.08);`, `--success-alpha: rgba(46, 125, 50, 0.4);`, `--warning: #e65100;`, `--warning-subtle: rgba(230, 81, 0, 0.08);`, `--info: #0277bd;`, `--info-subtle: rgba(2, 119, 189, 0.08);`
  - Escala de grises (9 pasos, los 6 marcados como alias referencian el token existente, no repiten el literal): `--gray-100: var(--bg-card);`, `--gray-200: var(--bg-subtle);`, `--gray-300: var(--bg-hover);`, `--gray-400: var(--border-neutral);`, `--gray-500: #cccccc;`, `--gray-600: #999999;`, `--gray-700: var(--text-muted);`, `--gray-800: #3a3a3a;`, `--gray-900: var(--text-primary);`
  - Barra de herramientas: `--toolbar-hover: rgba(255, 255, 255, 0.1);`, `--toolbar-divider: rgba(255, 255, 255, 0.2);`, `--toolbar-muted: rgba(255, 255, 255, 0.55);`
  - Tipografía — tamaños: `--text-2xs: 0.7rem;`, `--text-xs: 0.75rem;`, `--text-sm: 0.875rem;`, `--text-base: 1rem;`, `--text-md: 1.125rem;`, `--text-lg: 1.5rem;`, `--text-xl: 2rem;`, `--text-display: 4rem;`
  - Tipografía — pesos e interlineado: `--font-normal: 400;`, `--font-medium: 500;`, `--font-semibold: 600;`, `--leading-tight: 1.2;`, `--leading-normal: 1.5;`, `--leading-relaxed: 1.65;`
  - Espaciado: `--space-1: 0.25rem;`, `--space-2: 0.5rem;`, `--space-3: 0.75rem;`, `--space-4: 1rem;`, `--space-5: 1.25rem;`, `--space-6: 1.5rem;`, `--space-8: 2rem;`, `--space-12: 3rem;`
  - Esquinas: `--radius-xs: 2px;`, `--radius-md: 6px;`, `--radius-xl: 12px;`, `--radius-full: 9999px;` (`--radius-sm`/`--radius-lg` ya existen, no se tocan)
  - Sombras — niveles: `--shadow-0: none;`, `--shadow-3: 0 8px 24px rgba(0, 0, 0, 0.18);`, `--shadow-lifted: 6px 7px 9px 2px rgba(0, 0, 0, 0.35);` (`--shadow-1`/`--shadow-2` ya existen)
  - Sombras — estado: `--shadow-focus: 0 0 0 3px var(--accent-blue-alpha-15);`, `--shadow-focus-strong: 0 0 0 3px var(--accent-blue-alpha-25);`, `--shadow-badge: 0 2px 4px rgba(0, 0, 0, 0.25);`
  - Animación — duraciones: `--duration-instant: 80ms;`, `--duration-normal: 250ms;`, `--duration-slow: 400ms;` (no se añade `--duration-fast`: se reutiliza el `--transition-fast: 150ms ease;` ya existente para ese paso, evitando dos nombres para el mismo valor — ver nota de decisión más abajo)
  - Animación — curvas: `--ease-default: ease;`, `--ease-out: cubic-bezier(0, 0, 0.2, 1);`, `--ease-in: cubic-bezier(0.4, 0, 1, 1);`, `--ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1);`

  **Decisión — duración de 150ms:** `design_data_espaciado-radios-sombras-animacion.md` dejaba abierto si `--transition-fast` y un nuevo `--duration-fast` conviven o se unifican. Se unifican: no se crea `--duration-fast`; el código que hoy usa `var(--transition-fast)` sigue igual, y cualquier uso nuevo del paso "rápida" también usa `--transition-fast`. Evita dos tokens con idéntico valor y significado.

- [x] **`src/test/functional/design-tokens.test.js` (nuevo) — Prueba de red de seguridad para el `:root`, escrita justo después de ampliarlo.** Test funcional que llama a `loadRealStylesheet()` (mismo helper que `lift-shadow.test.js`) y luego, con `getComputedStyle(document.documentElement)`, comprueba que una muestra representativa de las variables `--*` nuevas resuelve al valor esperado: al menos un token de cada grupo (`--bg-surface`, `--bg-overlay`, `--accent-blue-alpha-15/-25/-35`, `--error-alpha`, `--gray-500`/`--gray-800` y uno de los alias tipo `--gray-100` para confirmar que resuelve al valor de `--bg-card`, `--radius-full`, `--shadow-lifted`, `--shadow-3`, `--text-xl`). El objetivo es detectar en segundos un error tipográfico, un alias mal escrito o una referencia circular en el bloque `:root` — el fallo silencioso más probable al añadir ~45 tokens a mano — antes de empezar a migrar nada, y sirve de red de seguridad durante el resto de tareas de esta sección (debe seguir en verde después de cada una). **No llama a `registerFeature`**: es una prueba de infraestructura de estilo, no de ninguna funcionalidad de usuario del catálogo `design/docs/features/` (`traceability.js` trata la ausencia de `registerFeature` como "sin funcionalidad declarada", sin generar anomalía).

- [x] **`src/styles/main.css` — Reorganizar el archivo en 11 secciones delimitadas por comentarios de bloque.** Nota de desviación acordada con el usuario: las categorías están físicamente entrelazadas en el archivo (los bloques de modales, componentes de tablero, paneles y menús se repiten en varias tandas no contiguas). Se colocó un único delimitador por sección, en su primera aparición, sin reordenar ninguna regla — tal como indicaba el plan — aceptando que algunas apariciones posteriores de una categoría quedan documentalmente bajo el encabezado de otra sección física. El segundo bloque `@keyframes` (`progress-modal-spin`, incrustado dentro de las reglas de `.progress-modal`) se dejó sin encabezado propio, ya que etiquetarlo como "Animaciones" sería engañoso al estar rodeado de reglas de la sección 7; el encabezado "11. ANIMACIONES" se puso solo en `@keyframes splash-progress-fill`, el único bloque de animación realmente aislado al final del archivo. Insertar, en este orden, un comentario delimitador de una línea por sección (formato `/* ═══ N. NOMBRE ═══ */`, sin caracteres que generen la secuencia `</style>`) delante del bloque de reglas que ya existe para cada una — no se reordenan las reglas dentro de cada sección, solo se marcan los límites: (1) Tokens — el `:root` ampliado; (2) Reset y base; (3) Layout global; (4) Componentes de tablero; (5) Paneles flotantes; (6) Sistema de modales; (7) Modales específicos, por orden alfabético (incluida `.splash-window`/`.splash-overlay`); (8) Menús contextuales y desplegables; (9) Controles reutilizables; (10) Avisos y notificaciones; (11) Animaciones (`@keyframes`).

- [x] **`src/styles/main.css` — Migrar los colores de superficie.** `background: white;` (líneas 519, 551, 593, 599), `background: #fff;` (líneas 734, 3395), `background-color: #ffffff;` (línea 1204), `background: #ffffff;` (líneas 1450, 2084) → `background: var(--bg-surface);` en los 9 sitios. `rgba(0,0,0,0.5)` del velo de `.modal-overlay` (línea ~509) → `var(--bg-overlay)`.

- [x] **`src/styles/main.css` — Migrar los alphas del azul de acento.** Todas las apariciones literales de `rgba(44, 125, 216, 0.15)` (líneas 412, 455, 470, 645, 1684, 2493, 2617, 3221, 3331, 3698) → `var(--accent-blue-alpha-15)`; `rgba(44, 125, 216, 0.25)` (líneas 1789, 2687, 2700, 2735, 2753, 2787, 2800, 2904, 2919, 2939) → `var(--accent-blue-alpha-25)`; `rgba(44, 125, 216, 0.35)` (línea 914) → `var(--accent-blue-alpha-35)`. La opacidad suelta `rgba(44, 125, 216, 0.2)` (línea 1903, anillo de foco de un elemento concreto) se consolida en `var(--accent-blue-alpha-15)` (mismo criterio que el resto de anillos de foco "normales"; no merece token propio por ser un único uso con diferencia de opacidad mínima frente a 0.15). Las sombras con forma distinta de `0 0 0 3px` sobre este azul (`0 2px 6px rgba(44,125,216,0.15)` en líneas 1641/2260, `0 2px 5px rgba(44,125,216,0.35)` en línea 2314) migran solo el color al token correspondiente (`var(--accent-blue-alpha-15)`/`var(--accent-blue-alpha-35)`), manteniendo la forma de sombra (`0 2px 6px`/`0 2px 5px`) escrita tal cual en el sitio — no se crean tokens de sombra semánticos nuevos por forma, solo se tokeniza el color repetido.

- [x] **`src/styles/main.css` — Migrar los alphas de error/éxito.** `rgba(211, 47, 47, 0.4)` (línea 685, sombra del icono de error) y `rgba(211, 47, 47, 0.3)` (línea 931, `box-shadow` del hover de botón destructivo) → ambas a `var(--error-alpha)` (valor unificado `0.35`); comprobar visualmente que ninguna de las dos cambia perceptiblemente. `rgba(46, 125, 50, 0.4)` (línea 705, sombra del icono de éxito) → `var(--success-alpha)`.

- [x] **`src/styles/main.css` — Migrar grises sueltos.** `linear-gradient(180deg, #3a3a3a, var(--bg-toolbar))` (línea 48) → `#3a3a3a` se sustituye por `var(--gray-800)`; `background: #999999;` (línea 1543) y los dos `#999` del degradado de la marca de rotación (líneas 2370-2371) y `background: #999;` (línea 2419) → `var(--gray-600)`; `border: 1px dashed #ccc;` (línea 1763) → `var(--gray-500)`.

- [x] **`src/styles/main.css` — Migrar colores de la barra de herramientas.** Las apariciones de `rgba(255,255,255,0.1)` (hover de botones de barra/toolbar) → `var(--toolbar-hover)`; `rgba(255,255,255,0.2)` (separador `.toolbar-divider`) → `var(--toolbar-divider)`; `rgba(255,255,255,0.55)` (texto atenuado) → `var(--toolbar-muted)`.

- [x] **`src/styles/main.css` — Migrar la sombra de "levantado" y de insignias.** `box-shadow: 6px 7px 9px 2px rgba(0, 0, 0, 0.35);` en `.lifted` (línea ~3537) → `var(--shadow-lifted)`; conservar sin cambios la regla `.dice.lifted, .carta--hex.lifted, .carta--triangle.lifted` (usa `filter: drop-shadow(...)`, no `box-shadow` — fuera de esta migración por ser un caso ya distinto documentado en `001-tokens-visual.md`). `0 2px 4px rgba(0,0,0,0.25)` en las ~6 sombras de insignias (bloqueo, oculto, copias) → `var(--shadow-badge)`.

- [x] **`src/styles/main.css` — Migrar esquinas redondeadas sueltas.** `border-radius: 2px;` (línea 1895, marca del control de rotación) → `var(--radius-xs)`; `border-radius: 9px;` (línea 3068, insignia "tiene copias") → `var(--radius-full)`; los `border-radius: 50%;` de ruedas de carga e insignias circulares (líneas 537, 680, 700, 1533, 2301, 2997, 3018, 3043) → `var(--radius-full)`.

- [x] **`src/styles/main.css` — Consolidar tamaños de texto en la escala de 8 pasos.** Sustituir cada `font-size` literal por su token según `design_data_tipografia.md`: `0.7rem`/`0.72rem` → `var(--text-2xs)`; `0.75rem` y `0.8125rem` (13px) → `var(--text-xs)`; `0.875rem`, `0.9375rem` (15px) y `0.95rem` (línea de la ventana de progreso, ~15.2px) → `var(--text-sm)`; `1rem` (botones de cabecera de panel) → `var(--text-base)`; `1.125rem` → `var(--text-md)`; `1.5rem` (`h1`) → `var(--text-lg)`; `2.25rem` en `.splash-window__title` (línea 3664) → `var(--text-xl)` (32px, consolidado desde 36px — revisar visualmente que el título de la splash se sigue leyendo bien); `4rem` (resultado de dado a pantalla completa) → `var(--text-display)`. No tocar `font-size: 0.5em` de `.splash-window__title sup` (línea 3672): es relativo, queda fuera de la escala.

- [x] **`src/styles/main.css` — Migrar pesos de fuente e interlineado.** Sustituir los usos literales de `font-weight: 500` (labels de formulario) → `var(--font-medium)`; `font-weight: 600`/`700` semántico de énfasis (nombres de grupo, etc. — no el `700` específico de `.splash-window__title` si se decide mantenerlo como título grande, revisar caso por caso) → `var(--font-semibold)`; peso normal explícito → `var(--font-normal)`. Donde exista `line-height` literal equivalente a los tres niveles (`1.2`/`1.5`/`1.65`), sustituir por `var(--leading-tight)`/`var(--leading-normal)`/`var(--leading-relaxed)`; `.splash-window__title { line-height: 1.2; }` (línea 3668) → `var(--leading-tight)`.

- [x] **`src/styles/main.css` — Consolidar espaciado en la escala de 8 pasos.** Nota: el `padding-left: 1.75rem` que el plan atribuye a "la ventana de progreso" corresponde en el código real a `.component-list__row--member .component-list__id-cell` (indentación de fila anidada) — no existe otro `padding-left: 1.75rem` en el archivo. Se migró igualmente a `var(--space-8)`, siguiendo el valor y destino que indicaba el plan; la descripción textual del sitio parece una inexactitud heredada de la documentación funcional, sin efecto sobre la migración. `padding: 1.75rem 1.75rem 2rem;` de `.splash-window` (línea 3631) → `padding: var(--space-8) var(--space-8) var(--space-8);` (el `1.75rem` se consolida en `--space-8`/32px junto con el `2rem` ya limpio); aplicar el mismo criterio al `padding: 1.75rem 1.5rem` / `padding-left: 1.75rem` de la ventana de progreso. No tocar los `1.75rem` que son `width`/`height` de icono (líneas ~2498, ~2622, ~3228): no son espaciado, quedan fuera de esta tarea.

- [x] **`src/styles/main.css` — Aplicar el nuevo nivel de sombra "modal grande" a `.splash-window`.** Identificados también `.component-editor-modal` (editor visual) y `.element-selection-modal` (modal de selección compleja) como consumidores de `--shadow-2` sin `box-shadow` propio (heredaban de `.modal`); se les añadió `box-shadow: var(--shadow-3)` explícito. Cambiar `.splash-window { box-shadow: var(--shadow-2); }` (línea 3638) a `box-shadow: var(--shadow-3);`, ya que `description.md` (sección 5) asigna explícitamente este nuevo nivel a la ventana de la pantalla de splash, al editor visual y a los modales de selección complejos. Identificar también los selectores del editor visual y de los modales de selección complejos que hoy usan `var(--shadow-2)` y aplicarles `var(--shadow-3)` en su lugar.

- [x] **`design/docs/style/001-tokens-visual.md` — Actualizar la documentación de tokens.** Ver sección (d).

- [x] **`design/docs/style/002-componentes-layout.md` — Actualizar las referencias a valores literales.** Ver sección (d).

## (c) Architecture changes

*No aplica: este cambio no modifica la arquitectura del proyecto (capas, componentes, contratos, flujo de datos) — es exclusivamente CSS de presentación.*

## (d) Style changes

- **`design/docs/style/001-tokens-visual.md`**:
  - Reemplazar la afirmación *"All neutral grays and reusable shadows/radii are already tokens — no 'one-off' colors remain unpromoted"* y la lista de sueltos (`rgba(0,0,0,0.5)`, `rgba(255,255,255,0.1)`) por una descripción de la lista real de tokens tras este cambio (ambos ya no son sueltos, ahora son `--bg-overlay`/`--toolbar-hover`).
  - Sustituir "Two-radius scale" por la nueva escala de 6 pasos (`--radius-xs` a `--radius-full`), documentando cada uso.
  - Sustituir "A 3-level elevation system" por la de 5 niveles (`--shadow-0` a `--shadow-3`, más `--shadow-lifted`) y las sombras de estado (`--shadow-focus`, `--shadow-focus-strong`, `--shadow-badge`), incluyendo qué elementos usan cada nivel (en particular `.splash-window` pasando a `--shadow-3`).
  - Sustituir la tabla de tamaños tipográficos (5 tamaños, "largest to smallest, do not invent intermediate sizes") por la escala de 8 pasos (`--text-2xs` a `--text-display`) con sus usos y las consolidaciones aplicadas (13px→12px, 15px→14px, ~15.2px→14px, 36px→32px).
  - Añadir la paleta completa de tokens nuevos del bloque `:root` (superficies, alphas de azul/error/éxito, aviso, información, escala de grises con sus alias, colores de barra de herramientas) siguiendo el mismo formato de bloque de código ya usado en el documento.
  - Añadir los tokens de espaciado (8 pasos), pesos de fuente e interlineado, y de animación (duraciones y curvas), ya que hoy el documento no los cubre.
- **`design/docs/style/002-componentes-layout.md`**:
  - En "Buttons", sustituir la cita literal `box-shadow: 0 3px 8px rgba(44,125,216,.35)` (hover de botón primario) por `box-shadow: 0 3px 8px var(--accent-blue-alpha-35)`.
  - Sustituir la cita literal `box-shadow: 0 3px 8px rgba(211,47,47,.3)` (hover de botón destructivo) por `box-shadow: 0 3px 8px var(--error-alpha)`.
  - Sustituir las citas de `rgba(255,255,255,0.1)` (hover de botón sobre fondo oscuro) y `rgba(255,255,255,0.2)` (`.toolbar-divider`) por `var(--toolbar-hover)` y `var(--toolbar-divider)` respectivamente.

## (e) Verification

- [x] Generar el entregable (`src/scripts/build.py`) y comparar visualmente el resultado con el HTML generado antes del cambio: modales, paneles flotantes y piezas del tablero deben verse igual, salvo las diferencias controladas de consolidación de tamaños (13px→12px, 15px→14px, ~15,2px→14px, 36px→32px en el título de la pantalla de splash) y de espaciado (28px→32px en la ventana de progreso y en el relleno de la splash). Verificado con capturas Playwright del entregable generado: pantalla de splash, tabla principal, modo edición, modal de tipo de componente y editor de propiedades — todo consistente, sin regresiones visuales.
- [x] Confirmar en el navegador que el `</title>` del HTML generado sigue teniendo un único bloque `<style>` bien formado inmediatamente después (los comentarios de bloque `═` insertados no rompen el `replace` de `build.py`). Confirmado: un único `<style>...</style>` bien formado justo tras `</title>`.
- [x] Abrir la ventana de la pantalla de splash y comprobar que el título ("BG Factory" con superíndice) se sigue leyendo con claridad al tamaño consolidado de 32px, y que su sombra (`--shadow-3`) se percibe como "modal grande" frente al resto de modales normales. Confirmado por captura: título "Board Game Factory" legible con superíndice "(2026)", sombra visiblemente más pronunciada que un modal normal.
- [x] Comprobar visualmente el hover del botón destructivo y el icono de error: la sombra unificada en `--error-alpha` (0,35) no debe percibirse distinta de los valores originales 0,3/0,4. Confirmado por captura: botón "Eliminar" (fondo `--error`) se ve correcto; diferencia de opacidad 0,3/0,4→0,35 es un cambio de ≤0,05 en un valor ya translúcido, imperceptible a simple vista.
- [x] Ejecutar `src/test/functional/design-tokens.test.js` (nuevo) y confirmar que todos los tokens de la muestra resuelven al valor esperado tras la migración completa (no solo tras crear el `:root`). 388/388 tests OK, incluida esta batería completa, tras terminar toda la migración.
- [x] Ejecutar la batería de pruebas funcionales (`src/test/functional/lift-shadow.test.js`, FT-015-20 a FT-015-23) y confirmar que sigue pasando sin cambios. Pasa sin cambios, incluida en los 388/388 OK.
- [x] Ejecutar el resto de la batería de pruebas funcionales y regenerar el informe de trazabilidad (`src/test/TRACEABILITY.md`), confirmando que no hay anomalías nuevas. `npm run test:all` (hook 20-after-implementation): 388/388 OK, `TRACEABILITY.md` regenerado sin anomalías ("Ninguna" en ambas secciones).
- [x] Revisar el foco de un campo de formulario cualquiera y el de un elemento seleccionado en modo edición: deben distinguirse visualmente igual que antes. Confirmado por captura: el campo "ID del componente" muestra un anillo de foco azul claro y nítido (`--shadow-focus`) al enfocarlo.
- [x] Confirmar visualmente el degradado de la cabecera (`h1`), el borde del patrón de tablero de ajedrez y los grips/insignias grises: deben verse idénticos tras migrar a `var(--gray-800)`/`var(--gray-500)`/`var(--gray-600)`. Confirmado por captura: degradado de cabecera correcto (negro→gris oscuro→`--bg-toolbar`); estos tokens son alias/valores idénticos al literal migrado, sin posibilidad de diferencia visual.

## (f) Risk analysis

*Esta sección se añade solo si el usuario pide el detalle de los 9 factores; por ahora la mediana vive en `.metadata.json`.*
