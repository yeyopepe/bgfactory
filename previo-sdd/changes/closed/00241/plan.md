- **Creation date**: 2026-09-06

## (a) Functional notes

**Out of scope:**
- La funcionalidad 026 (menú contextual en modo juego) — es el cambio 00240, aparte.
- Las ~31 funcionalidades hoy sin ningún test. No se crean baterías nuevas fuera de las 8 listadas (002, 005, 016, 022, 029, 032, 036, 039).
- El motor de tests (`src/test/harness.js`, `src/test/run.js`, `src/test/traceability.js`, `src/test/runner-page.html`, `src/test/setup.js`) no se toca. Ampliar `expect` con nuevos matchers queda descartado en este cambio: cada caso se escribe con los matchers ya existentes (`toBe`, `toEqual`, `toBeTruthy`, `toBeFalsy`, `toBeNull`, `toContain`, `toHaveLength`, `toBeGreaterThan`, `toThrow`). Para "≥ n" se usa `toBeTruthy()` sobre la expresión `a >= n` o `toBeGreaterThan(n-1)` cuando el valor es entero.
- `src/test/TRACEABILITY.md` no se edita a mano: lo regenera `run.js` → `generateTraceability` al ejecutar `npm test`. Tras implementar, se ejecuta `npm test` una vez para regenerarlo y que quede en el commit con la cobertura nueva.
- Las fichas funcionales de `previo-sdd/design/docs/features/` no se modifican (están completas y correctas; el contrato del framework dice explícitamente que esas fichas nunca se editan para registrar tests).
- No se persigue arreglar bugs: si un caso nuevo falla contra el código real, se deja documentado y se trata por las vías del proyecto (fix/change) fuera de este cambio. Ver "Riesgo de destapar fallos" abajo.

**Doubts resolved with the user:**
- *¿Reescribir los tests actuales o solo añadir?* → Solo añadir casos nuevos a cada fichero existente, conservando los actuales y su numeración. `synced-copies.test.js` gana casos `FT-005-02+` y además casos `FT-022-01+` propios (hoy solo aporta cobertura secundaria a la 022); se decide en implementación si la batería de la 022 vive en `synced-copies.test.js` o en un fichero nuevo `carta.test.js` — ver tarea correspondiente.
- *¿Nivel de cada caso?* → El que corresponda: nivel estado cuando basta con funciones públicas de `core/*`; nivel interfaz (`mountEditMode`/`mountPlayMode`) cuando depende del DOM o de un evento real. Mismo criterio que la batería actual.
- *¿Se toca `TRACEABILITY.md`?* → No, se regenera.
- *Riesgo de destapar fallos* → Aceptado. El objetivo es la cobertura; los fallos que afloren se tratan aparte.

### Inconsistencia detectada entre documentación y código (menor)

`previo-sdd/design/docs/architecture/011-functional-test-framework.md`, tabla "Files and responsibilities", fila `src/test/fixtures/*.json`:

- **Dice**: fixtures en formato `buildComponentsExport` `{ version, components, resources, tags, componentGroups, appTitle }`; y `errantes-componentes.json`, `mazo-repetido.json` como stress fixtures.
- **Código real**: los ficheros presentes son `src/test/fixtures/errantes-componentes.json` y `src/test/fixtures/mazo-repetido.json` (nombres correctos), pero `buildComponentsExport` hoy produce la clave `componentGroups` mientras que el propio doc, más abajo, y `parseState`/`parseImportedComponents` mantienen el alias legacy `groups`/`decks`. No es una divergencia de comportamiento, solo el doc cita el nombre de una constante (`buildComponentsExport` format) sin señalar que `loadFixture` pasa por `parseImportedComponents` (que tolera el alias). 
- **Suggested doc change**: en la tarea de cierre, si al escribir los casos de 032/036 se confirma el formato exacto que `loadFixture` acepta hoy, añadir una nota de una línea a esa fila de `011-*.md` aclarando que `loadFixture(name)` → `parseImportedComponents` (no `parseState`), y por tanto un fixture sin `version` o con `version` distinta se carga igual. `pv-do` lo aplica en su paso de actualización de documentación **solo si** la implementación lo confirma; si no se toca ningún fixture nuevo, se deja como está.

## (b) Technical solution

Cada tarea añade casos a un fichero de `src/test/functional/`. Los casos van al final del `describe` existente, con el siguiente `FT-NNN-nn` correlativo. Todos comparten la convención ya usada: `it('FT-NNN-nn · <descripción en minúscula>', () => { ... })`, `beforeEach(resetState)` a nivel de `describe`, imports relativos desde `../harness.js` / `../helpers.js` / `../../core/*` / `../../ui/*` / `../../modes/*`.

- [x] **`src/test/functional/component-crud.test.js` — añadir casos 002 (`FT-002-04` … `FT-002-12`), nivel estado salvo los marcados ui.**
  - `FT-002-04` · un id vacío no es válido: replica la comprobación de `validateId` (`src/ui/componentModal.js:817`) — que es una closure privada de la modal y no se puede importar — con la expresión `getComponents().some((c) => c.id === newId && c.id !== (component?.id ?? ''))` y `newId.trim() === ''`. Crear un componente, comprobar que con `id` `''` o solo espacios la expresión de validez da `false` (id vacío) y con un id no usado da `true`.
  - `FT-002-05` · un id duplicado de otro componente no es válido: añadir dos componentes con ids distintos; comprobar que la expresión de validez con el id del segundo, editando el primero, da `false`; y que con un id libre da `true`. (Se prueba la *regla*, no la UI: la validación vive en la capa UI como closure, así que el test reproduce la condición exacta y deja constancia de ella.)
  - `FT-002-06` · `createComponent()` genera siempre un `id` no vacío y `order`/`copyOf`/`groupId` con sus defaults (`order: null` antes de `addComponent`, `copyOf: null`, `groupId: null`, `sincronizado: true`, `bloqueado: 'ninguno'`, `accionClickDerecho: 'ninguno'`). Importar `createComponent` de `../../core/component.js`.
  - `FT-002-07` · `createDefaultComponent(tipo)` para cada uno de los 7 tipos (`'texto'`, `'tableroSimple'`, `'tableroPersonalizado'`, `'dado'`, `'documento'`, `'carta'`, `'mazo'`) devuelve un componente con ese `type` y con `properties` no vacío para los que lo fijan (todos menos `'texto'`). Ampliación de `FT-002-02` (que solo cubría 5 tipos "básicos") a los 7. Nota: los nombres de tipo internos son los del código; las etiquetas visibles de la modal previa ("Cuadro de texto", "Dado Configurable", …) son i18n y no se testean aquí (no hay función pública que las exponga sin montar la modal).
  - `FT-002-08` · el `type` no es editable tras crear: `updateComponent(c, { type: 'otro' })` — comprobar el comportamiento real de `updateComponent` (`src/core/component.js:55`) respecto a `type`. Si `updateComponent` deja pasar `type`, el caso documenta que la inmutabilidad la garantiza la UI (la modal no ofrece cambiarlo), igual que en `FT-002-04/05`; ajustar la aserción a lo que el código haga (no forzar un fallo).
  - `FT-002-09` · editar una propiedad general de un componente existente y persistirla en estado: `replaceComponent(c.id, updateComponent(c, { bloqueado: 'juego' }))`, comprobar `getComponents()[0].bloqueado === 'juego'`.
  - `FT-002-10` · `addComponent` asigna `order = 1` al nuevo y desplaza los demás +1 (varios componentes: el último añadido tiene `order === 1`, el primero `order === n`). Cubre la regla de apilado de `002`/`012` desde el alta.
  - `FT-002-11` · `removeComponent` recompacta `order` a 1..n contiguo (añadir 3, borrar el del medio, comprobar que los `order` restantes son `[1, 2]`).
  - `FT-002-12` (ui) · `mountEditMode()` con un componente en estado lo pinta en `#content` (un nodo con la clase del tipo, p. ej. `.carta`); tras `removeComponent` + `mountEditMode()` de nuevo, ya no está. Verifica el ciclo alta→pintado→borrado→no pintado a nivel interfaz.

- [x] **`src/test/functional/synced-copies.test.js` — añadir casos 005 (`FT-005-02` … `FT-005-09`).** Imports ya presentes: `createCopy`, `updateComponent` de `../../core/component.js`; `getComponents`, `addComponent`, `replaceComponent` de `../../core/state.js`. Añadir `removeComponent` al import de `state.js`.
  - `FT-005-02` · el id de una copia es `${originalId}-COPY-001` para la primera; una segunda copia del mismo original es `-COPY-002`. Usar `createCopy(original, getComponents())` dos veces (con `addComponent` entre medias).
  - `FT-005-03` · al borrar una copia se reutiliza su hueco: crear copias 001, 002, 003; borrar la 002 con `removeComponent`; la siguiente `createCopy` vuelve a ser `-COPY-002` (primer entero libre, `nextCopyId` en `src/core/component.js:114`).
  - `FT-005-04` · cambiar el `id` del original renombra en cascada los ids de sus copias conservando el sufijo: `replaceComponent(original.id, updateComponent(original, { id: 'nuevo' }))`; comprobar que la copia pasa a `nuevo-COPY-001` y su `copyOf` a `'nuevo'` (lógica en `replaceComponent`, `src/core/state.js:70`, vía `renameCopyId`).
  - `FT-005-05` · qué se sincroniza al editar el original: cambiar `name`, `width`, `height` y una propiedad de diseño del tipo en el original → la copia los adopta. Cerrar el bucle de `FT-005-01` (que solo probaba `colorFondo` de la cara) con más campos de la lista de `syncCopyWithOriginal` (`src/core/component.js:173`).
  - `FT-005-06` · qué NO se sincroniza: mover la copia (`x`/`y` propios), y comprobar que editar el original no revierte la posición de la copia; y que `properties.caraActual` de una carta-copia (clave de `NON_SYNCED_PROPERTY_KEYS`, `src/core/component.js:95`) es independiente.
  - `FT-005-07` · `bloqueado`/`oculto` con `sincronizado: true` siguen al original: poner `bloqueado: 'juego'` en el original → la copia sincronizada queda `bloqueado: 'juego'`. Con la copia a `sincronizado: false` (vía `updateComponent(copia, { sincronizado: false })` + `replaceComponent`), un cambio de `bloqueado` en el original ya no toca a la copia.
  - `FT-005-08` · borrado en cascada: `removeComponent(original.id)` elimina también todas sus copias (`getComponents()` queda sin ninguna). Y `removeComponent(copia.id)` no afecta al original ni a otras copias.
  - `FT-005-09` · no hay copias de copias a nivel de dato: `createCopy` sobre una copia produce un objeto cuyo `copyOf` apunta a la copia; el caso documenta que la app impide llegar aquí desde la UI (`ui/componentList.js` oculta "Copiar" en filas que ya son copia) — aserción sobre el hecho observable, sin forzar fallo. (Alternativa si se prefiere no depender de UI: omitir este caso y notarlo como cobertura de `003`/`005` a nivel interfaz, fuera de alcance.)

- [x] **Batería de la funcionalidad 022 (carta) — `FT-022-01` … `FT-022-08`.** Elegida opción (B): fichero nuevo `src/test/functional/carta.test.js` con `registerFeature({ primary: 22 })`. `synced-copies.test.js` mantiene su `registerFeature({ primary: 5, secondary: [22] })`. Decidir en implementación: (A) añadirlos a `synced-copies.test.js` (que ya declara `registerFeature({ primary: 5, secondary: [22] })` — habría que dejar `secondary: []` y crear un `describe` propio "022 — Componente carta", pero **un fichero solo declara un `primary`**, así que 022 seguiría siendo secundaria y no saldría como cobertura principal en `TRACEABILITY.md`); (B) **crear `src/test/functional/carta.test.js`** con `registerFeature({ primary: 22 })` propio. **Recomendado (B)** — es la única forma de que la 022 figure con test principal en la trazabilidad. Imports: `describe`/`it`/`expect`/`beforeEach`/`registerFeature` de `../harness.js`; `resetState`, `mountPlayMode`, `mountEditMode` de `../helpers.js`; `getComponents`, `addComponent`, `replaceComponent` de `../../core/state.js`; `createDefaultComponent`, `DEFAULT_CARTA_PROPERTIES` de `../../ui/componentModal.js`; `updateComponent` de `../../core/component.js`; `getProporcionRatio`, `CARD_PROPORTIONS` de `../../core/cardProportions.js`.
  - `FT-022-01` · una carta nueva nace mostrando la cara **trasera**: `createDefaultComponent('carta').properties.caraActual === 'trasera'` (constante `DEFAULT_CARTA_PROPERTIES.caraActual`, `src/ui/componentModal.js:137`).
  - `FT-022-02` · proporción por defecto `'5:7'` y `width`/`height` coherentes: `component.width === 180` (`DEFAULT_CARTA_WIDTH`) y `component.height === 180 / getProporcionRatio('5:7')`.
  - `FT-022-03` (ui) · en modo juego, un click sobre la carta la voltea de trasera a frontal: `mountPlayMode()`, localizar `#content .carta.carta--clickable`, `dispatchEvent(new MouseEvent('click', { bubbles: true }))`, comprobar `getComponents()[0].properties.caraActual === 'frontal'`. Un segundo click vuelve a `'trasera'`. (El handler está en `src/ui/componentRenderer.js:1872`, gated por `isInteractionActive(component, 'voltear')` — activo por defecto.)
  - `FT-022-04` (ui) · el volteo funciona con la carta bloqueada: misma prueba que `FT-022-03` con `c.bloqueado = 'juego'` antes de `addComponent`. `bloqueado` solo afecta al arrastre (`canMove`), nunca al click de volteo.
  - `FT-022-05` · proporción `'circular'`: al crear/cambiar a `'circular'` la carta pasa a `width === height`. Verificar contra la lógica real de cambio de proporción (buscar en `src/ui/componentModal.js` el handler del `<select>` de proporción, ~línea 1711; si el ajuste `width=height` lo hace la UI y no una función pura, el caso reproduce ese cálculo con `getProporcionRatio('circular') === 1` y documenta que la UI lo aplica). `CARD_PROPORTIONS` marca `shape: 'circular'`.
  - `FT-022-06` · las proporciones hexagonales/triangulares tienen ratio fijo: `getProporcionRatio('hex-vertical') === Math.sqrt(3)/2`, `getProporcionRatio('triangulo') === 1`, y `CARD_PROPORTIONS.find(p => p.value === 'hex-vertical').shape === 'hex-vertical'` (silueta fija, no `'rect'`). `isRectShape('5:7') === true`, `isRectShape('circular') === false` — importar `isRectShape` de `cardProportions.js`.
  - `FT-022-07` (ui) · una cara sin diseño se pinta sin aviso: `mountPlayMode()` con una carta recién creada; el nodo `.carta` existe y no contiene ningún nodo de error/aviso. Comprobación mínima de "se pinta en blanco, sin romper".
  - `FT-022-08` · el diseño de cada carta es propio: dos cartas independientes; editar `properties.caraFrontal.bordeGrosor` de una vía `replaceComponent(id, updateComponent(c, { properties: { ...c.properties, caraFrontal: { ...c.properties.caraFrontal, bordeGrosor: 5 } } }))` no cambia el de la otra.

- [x] **`src/test/functional/hidden-in-play.test.js` — añadir casos 016 (`FT-016-02` … `FT-016-06`), nivel interfaz.** `FT-016-03` omitido a propósito (no se puede disparar `contextmenu` sobre un nodo que no se renderiza sin solaparse con 026). Clases confirmadas: `tableroSimple`→`.board`, `dado`→`.dice`, insignia candado `.component-lock-badge`. Imports ya presentes: `resetState`, `mountEditMode`, `mountPlayMode` de `../helpers.js`; `addComponent` de `../../core/state.js`; `createDefaultComponent` de `../../ui/componentModal.js`. Añadir `replaceComponent` de `state.js` y `updateComponent` de `../../core/component.js` donde haga falta.
  - `FT-016-02` · `oculto` es `false` por defecto: `createDefaultComponent('carta').oculto === false`; y una carta sin el campo (`delete c.oculto`) montada en modo juego se pinta (se comporta como `false`).
  - `FT-016-03` · un componente oculto no aparece en el menú contextual de modo juego: no se puede probar aquí sin solaparse con 026; **omitir** y notar que lo cubre 026 (cambio 00240) — el nodo del componente oculto ni se renderiza, así que no hay dónde disparar el `contextmenu`.
  - `FT-016-04` (ui) · en modo edición un componente oculto se sigue pudiendo seleccionar/mover: `mountEditMode()`, el `.carta` oculto existe y tiene la clase que lo hace arrastrable/seleccionable (`carta--movable` / `carta--selectable`, ver `src/ui/componentRenderer.js`). Contrapunto de `FT-016-01`.
  - `FT-016-05` (ui) · la insignia de oculto (`.component-hidden-badge`) y la de bloqueado conviven: carta con `oculto: true` **y** `bloqueado: 'juego'` en modo edición → el `.carta` contiene tanto `.component-hidden-badge` como la insignia de candado (localizar su clase real en `componentRenderer.js`: `createLockBadge`; buscar su `className`). Ambas presentes, una sola de cada.
  - `FT-016-06` (ui) · cubrir varios tipos, no solo carta: repetir la comprobación base de `FT-016-01` (oculto no se pinta en juego, sí en edición) para `'dado'` y `'tableroSimple'` — parametrizar con un bucle sobre tipos y su clase DOM (`.dice`, `.tablero-simple` / la que use el renderer; confirmar clases exactas en `componentRenderer.js`).

- [x] **`src/test/functional/autosave.test.js` — añadir casos 029 (`FT-029-03` … `FT-029-09`), nivel estado.** Imports ya presentes cubren casi todo; añadir de `../../core/state.js`: `getComponents`/`removeComponent`/`replaceComponent`, `setPanelState`/`getPanelState`, `addTag`/`getTags`, `setAppTitle`/`getAppTitle`. Reutilizar el helper local `persist()` del fichero (llama a `saveState` con los 10 args en orden).
  - `FT-029-03` · el estado guardado refleja una **edición** (no solo alta): crear componente, `persist()`, editar (`replaceComponent` con `updateComponent(c, { name: 'x' })`), `persist()`, releer `localStorage` → `parsed.components[0].name === 'x'`.
  - `FT-029-04` · el estado guardado refleja un **borrado**: crear 2, `persist()`, `removeComponent` de uno, `persist()`, releer → `parsed.components` tiene 1.
  - `FT-029-05` · se guarda y recupera el `panelState`: `setPanelState({ collapsed: true, width: 321 })`, `persist()`, `loadState()` → `saved.panelState.collapsed === true` y `.width === 321`. (`saveState` serializa `panelState` como 2º arg; `parseState` lo devuelve si es objeto.)
  - `FT-029-06` · se guardan y recuperan las **etiquetas**: `addTag(createTag({ name: 'Héroes' }))` (importar `createTag` de `../../core/tag.js`), `persist()`, `loadState()` → `saved.tags` contiene una con ese `name`.
  - `FT-029-07` · se guarda y recupera el **título de cabecera**: `setAppTitle('Mi partida')`, `persist()`, `loadState()` → `saved.appTitle === 'Mi partida'`. Y un guardado con `appTitle` vacío/ausente → `loadState()` devuelve `DEFAULT_APP_TITLE` (importar de `../../core/appTitle.js`): construir el JSON a mano en `localStorage` sin `appTitle` y comprobar.
  - `FT-029-08` · `loadState()` distingue **misma versión** vs **otra versión** vs **corrupto** vs **inexistente**. Cuatro sub-aserciones sobre `parseState` (vía `loadState()` tras escribir `localStorage.setItem('bgfactory:state', ...)` a mano):
    - clave ausente → `loadState() === null`.
    - JSON `{"version": <CURRENT_VERSION>, "components": []}` → objeto sin `error`.
    - JSON `{"version": 999999, "components": []}` → `{ error: 'version-mismatch' }`.
    - JSON `"{ no cierra" ` (texto no parseable) → `{ error: 'corrupt' }`.
    - JSON `{"version": <CURRENT_VERSION>}` (sin `components`) → `{ error: 'corrupt' }`.
    Importar `CURRENT_VERSION` (ya importado en el fichero).
  - `FT-029-09` · un guardado sin `panelState`/`tags` no invalida el estado: JSON `{"version": <CURRENT_VERSION>, "components": []}` → `loadState()` devuelve objeto con `panelState: null` y `tags: []` (defaults), sin `error`. Documenta el arranque de respaldo por-campo descrito en `007-persistence-build.md`.
  - **Nota**: la lógica de *qué toast se muestra* ante `version-mismatch`/`corrupt` vive en `main.js` (no cargado por el runner, decisión `test.decision.no-main-js`). Estos casos cubren `parseState`/`loadState`, que es hasta donde llega el nivel de test sin `main.js`. No se intenta probar el toast.

- [x] **`src/test/functional/export-import.test.js` — añadir casos 032 (`FT-032-02` … `FT-032-11`), nivel estado.** `nextImportedId` confirmado: `x-imported` si libre, si no `x-imported(2)`. Filas de informe: `tipoError` ∈ `'etiqueta'` (autocreada) / `'etiquetaDuplicada'` (reutilizada) / `'recurso'`. Imports ya presentes: `captureDownload`/`getLastDownload`/`injectFileImport`/`restoreAllMocks` de `../helpers.js`; `buildComponentsExport` de `../../core/persistence.js`; `downloadJson` de `../../core/fileExport.js`. Añadir `parseImportedComponents` de `persistence.js`, `mergeImportedGame` de `../../core/importMerge.js`, `getTags`/`addTag`/`getResources`/`addResource` de `state.js`, `createTag` de `../../core/tag.js`.
  - `FT-032-02` · el fichero exportado incluye `version` y `appTitle`, y solo los elementos pasados: `buildComponentsExport([c1], [], [], [], 'Mi juego')` → `.version === CURRENT_VERSION`, `.appTitle === 'Mi juego'`, `.components` tiene 1. (El filtrado por checkbox lo hace `ui/editModeToggle.js` antes de llamar; `buildComponentsExport` recibe listas ya filtradas — se prueba que respeta lo que recibe.)
  - `FT-032-03` · `parseImportedComponents` acepta un fichero de **otra versión**: `JSON.stringify({ version: 999999, components: [ {..} ] })` → resultado sin `error`, `.components` con 1. (A diferencia de `parseState`, `parseImportedComponents` no comprueba versión — `src/core/persistence.js:74`.)
  - `FT-032-04` · `parseImportedComponents` marca error en fichero **no válido**: `''` → `{ error: true }`; `'{ roto'` → `{ error: true, detail: <mensaje> }`; `'{"foo":1}'` (sin `components` array) → `{ error: true }`.
  - `FT-032-05` · modo `overwrite`: `mergeImportedGame({ mode: 'overwrite', conflictMode: 'overwrite', existingComponents: [ex1], existingResources: [], existingTags: [], selectedComponents: [imp1], selectedResources: [], selectedTags: [], allImportedResources: [], allImportedTags: [] })` → `result.components` contiene solo `imp1` (parte de listas vacías, `src/core/importMerge.js:33`).
  - `FT-032-06` · modo `add`, id nuevo: mismo `mergeImportedGame` con `mode: 'add'` y `imp1.id` distinto de `ex1.id` → `result.components` tiene ambos.
  - `FT-032-07` · modo `add`, id duplicado, `conflictMode: 'overwrite'`: `imp1.id === ex1.id` → el resultado tiene 1 componente, el importado reemplaza al existente.
  - `FT-032-08` · modo `add`, id duplicado, `conflictMode: 'keepBoth'`: → 2 componentes; el importado recibe id `${id}-imported` (`nextImportedId`, `src/core/importMerge.js:18`). Si `${id}-imported` también choca, `-imported(2)` — segunda sub-aserción con un `existingComponents` que ya contenga `${id}-imported`.
  - `FT-032-09` · tras la fusión, un componente importado que referencia una **etiqueta ausente** provoca su autocreación: `selectedComponents` con un componente cuyo `etiquetaIds: ['tag-x']` y `allImportedTags: [{ id: 'tag-x', name: 'Facción' }]`, sin esa etiqueta en `existingTags` → `result.tags` contiene una con id `'tag-x'` (o la reutilización si ya existía uno con nombre `'Facción'` — segunda sub-aserción). Report row con `tipoError: 'etiqueta...'`.
  - `FT-032-10` · un componente importado que referencia un **recurso ausente** se añade con la referencia a `null` y genera report: `selectedComponents` con un componente cuyo `properties.imagenResourceId: 'res-x'` no presente en `existingResources` ni `allImportedResources` → el componente entra, `imagenResourceId` queda `null`, `report` tiene una fila `tipoError: 'recurso'`. (Verificar la ruta exacta de la referencia contra `collectResourceRefsWithPath` en `importMerge.js`; usar una propiedad que esa función reconozca.)
  - `FT-032-11` · round-trip completo por descarga: `captureDownload()`, `addComponent(createDefaultComponent('mazo'))`, `downloadJson('j.json', buildComponentsExport(getComponents(), [], [], [], 'x'))`, `await getLastDownload()`, `resetState()`, `injectFileImport(dl.data)` → `getComponents()` reproduce el mazo. Amplía `FT-032-01` (que usaba `'carta'`) a otro tipo con `properties` no trivial. `afterEach(restoreAllMocks)` ya está en el fichero.

- [x] **`src/test/functional/fresh-boot.test.js` — añadir casos 036 (`FT-036-02` … `FT-036-05`), nivel estado.** Imports ya presentes: `getComponents`, `getResources`, `getResourcesSeeded`, `markResourcesSeeded`, `addResource` de `../../core/state.js`; `createResource` de `../../core/resource.js`; `DEFAULT_RESOURCES` de `../../data/defaultResources.js`.
  - `FT-036-02` · `DEFAULT_RESOURCES` tiene exactamente 2 entradas, una `type: 'imagen'` (id `example-image`) y una `type: 'tipografia'` (id `example-font`). Aserción directa sobre el array importado.
  - `FT-036-03` · tras "sembrar" (reproduciendo `seedDefaultResources`: `markResourcesSeeded()` + `addResource(createResource({ ...data }))` por cada `DEFAULT_RESOURCES`), `getResources()` tiene los 2 con sus ids fijos y `getResourcesSeeded() === true`. (Extiende `FT-036-01`, que solo comprobaba longitud.)
  - `FT-036-04` · los recursos de ejemplo son editables/eliminables como cualquier otro: tras sembrar, `removeResource('example-image')` (importar de `state.js`) → `getResources()` tiene 1; `replaceResource('example-font', updateResource(r, { name: 'X' }))` (importar `updateResource` de `resource.js`) → el nombre cambia. Demuestra que no tienen protección especial.
  - `FT-036-05` · con `resourcesSeeded` ya `true`, no se vuelve a sembrar: partir de `getResourcesSeeded() === false`, sembrar una vez, borrar `example-image`, y comprobar que una segunda pasada de la lógica "si `!getResourcesSeeded()` sembrar" **no** repone nada (la guarda es `getResourcesSeeded()`, que ya es `true`). Reproduce la tabla de `007-persistence-build.md` "Default resources and backfill".
  - **Nota**: `seedDefaultResources`/`backfillDefaultResourcesIfNeeded` viven en `main.js` (no cargado). Los casos reproducen su lógica con las funciones públicas de `state.js`, igual que ya hace `FT-036-01`.

- [x] **`src/test/functional/top-controls.test.js` — añadir casos 039 (`FT-039-03` … `FT-039-07`).** `createFitButton` usa `title = t('toolbar.fitZoom')` + `aria-label = t('toolbar.fitZoom.aria')`; `createSettingsButton` usa `title = t('toolbar.settings')`. Botón de modo: clase `.mode-switcher__mode-btn`. Imports ya presentes: `mountEditMode`, `mountPlayMode` de `../helpers.js`; `MODES`, `getState`, `setMode` de `../../core/state.js`; `on` de `../../core/eventBus.js`; `t` de `../../core/i18n.js`.
  - `FT-039-03` (ui) · en **modo juego** `#mode-switcher` contiene, en orden: Importar, Exportar, un separador (`.toolbar-divider`), el botón de modo con texto `t('toolbar.modeEdit')`, y los botones de ajustar zoom y configuración. Comprobar `textContent` contiene `t('toolbar.import')`, `t('toolbar.export')`, `t('toolbar.modeEdit')`; y `querySelector('.toolbar-divider')` existe; y hay un botón cuyo `title` es `t('toolbar.fitZoom')` y otro `t('toolbar.settings')`. (`renderModeSwitcher`, `src/ui/editModeToggle.js:285`.)
  - `FT-039-04` (ui) · en **modo edición** `#mode-switcher` NO tiene el bloque Importar/Exportar ni el separador (viven en `#edit-toolbar`): `switcher.textContent` no contiene `t('toolbar.import')`; `querySelector('.toolbar-divider')` es `null`; sí contiene `t('toolbar.modePlay')` y los botones de zoom/configuración. Y `#edit-toolbar` sí contiene `t('toolbar.import')` y `t('toolbar.export')` (ya cubierto parcialmente por `FT-039-02`; aquí se afina el reparto entre las dos zonas).
  - `FT-039-05` (ui) · el botón de modo cambia de texto según el modo pero está siempre en `#mode-switcher`: en `mountPlayMode()` su texto es `t('toolbar.modeEdit')`; en `mountEditMode()` es `t('toolbar.modePlay')`; en ambos casos el botón está dentro de `#mode-switcher`, no de `#content`.
  - `FT-039-06` (ui) · el botón "Ajustar zoom" existe en ambos modos con `title === t('toolbar.fitZoom')` y `aria-label`/`title` de `t('toolbar.fitZoom.aria')` según corresponda (verificar cuál se usa en `createFitButton`). No se prueba el efecto de `fitToBounds` (requiere geometría real de mesa, fuera de alcance de este caso — se puede notar como candidato a un test ui dedicado de la funcionalidad 001).
  - `FT-039-07` (ui) · el botón "Configuración" existe en ambos modos con `title === t('toolbar.settings')`. No se prueba que abra el modal (`openSettingsModal` monta su propia UI; un caso de la funcionalidad 038 lo cubriría) — aquí solo su presencia y etiqueta.

- [x] **Cierre — regenerar `TRACEABILITY.md`.** `npm test` → `Total: 66 — OK: 66 — FALLOS: 0`, exit 0. `TRACEABILITY.md` regenerado. Ningún caso nuevo falla contra el código real. No se tocó ningún fixture, así que la nota condicional a `011-*.md` (sección (c)) no aplica. Tras implementar todos los casos, ejecutar `npm test` desde la raíz del repo (si sale código `2`, ejecutar antes `npm run test:setup` y reintentar una vez). Esto regenera `src/test/TRACEABILITY.md` con la cobertura nueva. Si algún caso nuevo **falla** contra el código real: no arreglarlo aquí — anotarlo (fichero, caso, esperado/obtenido) para tratarlo como fix/change aparte; si el fallo es por un error del propio test, corregir el test. El objetivo del cambio es que la batería exista y sea correcta, no que el código pase el 100% a la primera. Comprobar que `TRACEABILITY.md` ya no lista como "sin ningún test" ninguna de las 8 funcionalidades y que la 022 figura con test **principal** (`FT-022-*`), no solo secundaria.

## (c) Architecture changes

Solo si la tarea de cierre confirma que hace falta la nota aclaratoria sobre `loadFixture` (ver "Inconsistencia detectada" en la sección (a)):

- **`previo-sdd/design/docs/architecture/011-functional-test-framework.md`** — en la tabla "Files and responsibilities", fila `src/test/fixtures/*.json`, añadir una frase: `loadFixture(name)` carga el fichero vía `parseImportedComponents` (no `parseState`), por lo que un fixture sin `version` o con `version` distinta a `CURRENT_VERSION` se carga igual. Solo si en implementación se toca/crea algún fixture y se verifica este camino; si no, no tocar el doc.

No hay ningún otro cambio de arquitectura: no se modifica el motor de tests, ni contratos, ni la estructura de `src/test/`. Se añaden ficheros/casos siguiendo el contrato ya documentado.

## (e) Verification

- [x] `npm test` termina con código `0` **o** con código `1` únicamente por casos nuevos que reflejan un fallo real del código de producción (documentados en la nota de cierre), nunca por casos mal escritos ni por anomalía de trazabilidad. → Termina con código `0` (`Total: 66 — OK: 66 — FALLOS: 0`).
- [x] `src/test/TRACEABILITY.md`, tras `npm test`, muestra tests en la fila de las 8 funcionalidades: 002 (con `FT-002-01`…`FT-002-12`), 005 (`FT-005-01`…`FT-005-09`), 016 (`FT-016-01`…`FT-016-06`, sin `FT-016-03` — omitido a propósito), 022 (**`FT-022-01`…`FT-022-08` como principal**, más `FT-005-*` como secundaria), 029 (`FT-029-01`…`FT-029-09`), 032 (`FT-032-01`…`FT-032-11`), 036 (`FT-036-01`…`FT-036-05`), 039 (`FT-039-01`…`FT-039-07`). Verificado en el fichero regenerado.
- [x] La sección "Funcionalidades sin ningún test" de `TRACEABILITY.md` ya no incluye ninguna de las 8.
- [x] La sección "Tests que declaran una funcionalidad inexistente" sigue vacía (`_Ninguna._`).
- [x] Cada fichero de `src/test/functional/` modificado conserva intactos sus casos previos y su `registerFeature`. `synced-copies.test.js` mantiene `registerFeature({ primary: 5, secondary: [22] })`; `carta.test.js` (nuevo) añade `registerFeature({ primary: 22 })`.
- [x] Ningún fichero de `src/test/harness.js`, `src/test/run.js`, `src/test/traceability.js`, `src/test/runner-page.html`, `src/test/setup.js` aparece en el diff de este cambio.
- [x] No se añadió ningún helper a `src/test/helpers.js` ni ningún fixture a `src/test/fixtures/` — no hizo falta.
- [x] Los cambios de este cambio quedan acotados a `src/test/functional/*.test.js` y `src/test/TRACEABILITY.md`. No se tocó `previo-sdd/design/docs/architecture/011-functional-test-framework.md` (nota condicional a tocar fixtures, no aplicada).

## (f) Risk analysis

| Factor | Value |
|---|---|
| Uso compartido | 1 |
| Alcance | 3 |
| Profundidad del cambio | 1 |
| Cobertura de tests | 0 |
| Criticidad del flujo | 0 |
| Reversibilidad | 0 |
| Datos persistentes | 0 |
| Superficie de seguridad | 0 |
| Datos sensibles | 0 |

**Median**: 0

### Risk median meaning

| Value | Meaning |
|---|---|
| 0 | No risk — fully isolated change, impossible for it to affect anything else |
| 1–2 | Minimal risk — local change, with a safety net (tests) or easily reversible |
| 3–4 | Low risk — touches some shared surface or several spots, but doesn't touch contracts or data |
| 5–6 | Moderate risk — shares code with other parts, partial test coverage, or touches a contract/signature used by others |
| 7–8 | High risk — deep change in heavily shared and/or untested code, in a relevant flow, persistent data, or security |
| 9 | Very high risk — structural change in a critical business flow, hard to revert, untested |
| 10 | Extreme risk — deep, broad change in critical, heavily shared code, untested, not easily reversible, touching data and/or security at once |
