- **Creation date**: 2026-09-06

## (a) Functional notes

**Out of scope:**
- No se toca ningún fichero de producción (`src/core/**`, `src/ui/**`, `src/modes/**`, `src/main.js`). Sólo se añaden ficheros nuevos bajo `src/test/functional/` y se regenera `src/test/TRACEABILITY.md` (lo hace `npm test` solo).
- No se refactoriza `main.js` para exportar `renderAppVersion` (funcionalidad 037): el caso de interfaz replica un render mínimo dentro del propio test. Se anotó como mejora posible pero queda fuera.
- No se corrige aquí la inconsistencia de `previo-sdd/design/docs/architecture/005-modes.md` líneas 10-12 (layout/textos de `#mode-switcher` previos a 00244). Se aplica en la sección (c) de este plan como actualización de documentación, no como cambio de código.
- Cobertura representativa de los contratos clave, no exhaustiva de cada matiz visual. Sin CSS en el runner: asserts sobre estado, clases, atributos y estructura DOM, nunca sobre medidas calculadas.
- El bloque `#app-title` (cabecera, con `formatVersion()`) NO se toca — ya lo cubre `app-title.test.js` (funcionalidad 030). Este cambio cubre `#app-version` (esquina inferior derecha), que es funcionalidad 037.

**Doubts resolved with the user:**
- *¿Cómo cubrir 037 si `main.js#renderAppVersion` no está exportada y el runner no carga `main.js`?* → Opción A: el caso de interfaz replica un `renderAppVersion` mínimo en el propio fichero de test (patrón ya usado en `fresh-boot.test.js` con `seedDefaultResources`); el resto a nivel estado.
- *¿Un fichero por funcionalidad o casos repartidos?* → Un fichero nuevo por funcionalidad.
- *¿Cobertura exhaustiva o representativa?* → Representativa de los contratos clave.

## (b) Technical solution

- [x] **`src/test/functional/infinite-table.test.js` — nuevo, funcionalidad 001 (nivel ui).** `import { describe, it, expect, beforeEach, afterEach, registerFeature } from '../harness.js'`; `import { resetState, mountEditMode, mountPlayMode, mountChrome } from '../helpers.js'`; `import { createInfiniteTable, fitToBounds } from '../../ui/table.js'`; `import { getComponentsBounds } from '../../ui/componentRenderer.js'`; `import { addComponent } from '../../core/state.js'`; `import { createDefaultComponent } from '../../ui/componentModal.js'`. `registerFeature({ primary: 1 })`. **Aislamiento de cámara**: `cameraX/cameraY/zoom` son estado de módulo de `table.js` NO reseteado por `resetState()`; `beforeEach(() => { resetState(); fitToBounds(null); })` (deja `translate(0px, 0px) scale(1)`) y `afterEach(() => fitToBounds(null))`. Para leer la cámara se parsea `worldEl.style.transform` (regex `translate\(([-\d.]+)px, ([-\d.]+)px\) scale\(([\d.]+)\)`), montando la mesa con `createInfiniteTable(document.createElement('div'))` y adjuntando `el` a `document.body` (necesario para que `getBoundingClientRect()` de `fitToBounds` mida). Casos:
  - `FT-001-01 · el arrastre de la mesa desplaza worldEl`: montar mesa, `viewport.dispatchEvent(new MouseEvent('mousedown', { button: 0, clientX: 100, clientY: 100, bubbles: true }))`, `document.dispatchEvent(new MouseEvent('mousemove', { clientX: 160, clientY: 130, bubbles: true }))`, `document.dispatchEvent(new MouseEvent('mouseup', { bubbles: true }))`; el `translate` de `worldEl.style.transform` refleja el delta (+60, +30) respecto al inicial.
  - `FT-001-02 · la rueda hace zoom y respeta el tope máximo`: varios `viewport.dispatchEvent(new WheelEvent('wheel', { deltaY: -100, clientX, clientY, bubbles: true, cancelable: true }))` (zoom in); el `scale` sube pero nunca supera `2.5`.
  - `FT-001-03 · la rueda respeta el tope mínimo`: varios `wheel` con `deltaY: 100`; el `scale` baja pero nunca por debajo de `0.5`.
  - `FT-001-04 · fitToBounds(null) deja la vista neutra`: tras un pan/zoom, `fitToBounds(null)` → `transform` == `translate(0px, 0px) scale(1)`.
  - `FT-001-05 · fitToBounds(bounds) reencuadra acotando al rango de zoom`: `fitToBounds({ minX: 0, minY: 0, maxX: 20, maxY: 20 })` (caja diminuta) → `scale` == `2.5` (tope máx, no infinito); `fitToBounds({ minX: 0, minY: 0, maxX: 100000, maxY: 100000 })` → `scale` == `0.5` (tope mín).
  - `FT-001-06 · el botón "Ajustar zoom" está en #mode-switcher en ambos modos`: `mountPlayMode()` y `mountEditMode()` (ambos llaman a `mountChrome()` → `renderModeSwitcher`); en cada modo, `document.getElementById('mode-switcher').querySelector('.mode-switcher__fit-btn')` existe.
  - `FT-001-07 · pulsar "Ajustar zoom" sin componentes deja la vista neutra`: `resetState()` + `fitToBounds(null)` deja algo no-neutro con un pan previo; montar mesa (adjunta a `document.body`), `mountPlayMode()`, `getComponentsBounds(getComponents())` es `null`; click en `.mode-switcher__fit-btn` → `worldEl.style.transform` == `translate(0px, 0px) scale(1)`.
  - `FT-001-08 · pulsar "Ajustar zoom" con componentes reencuadra`: `addComponent(createDefaultComponent('carta'))`, montar mesa adjunta a `document.body`, `mountEditMode()`; click en `.mode-switcher__fit-btn`; el `transform` de `worldEl` cambia respecto al estado previo y su `scale` queda dentro de `[0.5, 2.5]`. *(Nota de implementación: la mesa que pinta `mountEditMode()` dentro de `#content` es la `activeViewport` real que `fitToBounds` usa; basta con que `#content` esté en el DOM del runner — lo está. Verificar al implementar si hace falta adjuntar algo más a `document.body` para que `getBoundingClientRect()` dé tamaño no nulo; si el `scale` sale `NaN`/0, adjuntar `#content` o su contenedor con un tamaño explícito no es opción — en su lugar, montar la mesa propia con `createInfiniteTable` y un `div` dimensionado, y llamar directamente a `fitToBounds(getComponentsBounds(getComponents()))`, que es exactamente lo que hace el handler del botón.)*
- [x] **`src/test/functional/component-modal-tabs.test.js` — nuevo, funcionalidad 002 (nivel state + ui).** `import` de `harness.js` (`describe, it, expect, beforeEach, afterEach, registerFeature`); `import { resetState } from '../helpers.js'`; `import { getComponents, addComponent } from '../../core/state.js'`; `import { openComponentModal, createDefaultComponent } from '../../ui/componentModal.js'`. `registerFeature({ primary: 2 })`. `openComponentModal` añade `.modal-overlay` a `document.body`; `afterEach(() => { document.querySelectorAll('.modal-overlay').forEach((o) => o.remove()); resetState(); })`. Helper local para localizar la modal abierta: `const modal = () => document.body.querySelector('.modal-overlay .modal.component-editor-modal')`. Continuación de numeración desde `FT-002-13` (el máximo en `component-crud.test.js` es `FT-002-12`). Casos:
  - `FT-002-13 · la modal abre siempre en la pestaña "Generales"`: `openComponentModal({ component: createDefaultComponent('carta'), onAccept(){}, onDelete(){} })`; `modal().querySelectorAll('.modal__tab')` tiene longitud `5`; el que tiene la clase `.active` es el primero, y su `.content` asociado (mismo índice de `contentArea.children`) es el visible (`style.display !== 'none'`).
  - `FT-002-14 · pulsar cada pestaña muestra su panel y oculta los demás`: para `i` de 1 a 4, `modal().querySelectorAll('.modal__tab')[i].click()`; tras cada click, sólo `contentArea.children[i]` tiene `display: block` y el resto `display: none`; la pestaña `i` tiene `.active` y las demás no. (`contentArea` = `modal().querySelector('.modal__content')`.)
  - `FT-002-15 · el botón "Eliminar" aparece al editar, no al crear`: al abrir con `component` no nulo y `onDelete` pasado → `modal().querySelector('.btn-eliminar')` existe. Cerrar; abrir con `component: null` (o sin `onDelete`) → `modal().querySelector('.btn-eliminar')` es `null`. *(Nota: `openComponentModal` con `component = null` crea internamente un `workingComponent` tipo `'texto'`; `isNew` es `true` → sin botón Eliminar; es el camino que en la app usa `componentTypeModal` antes. Verificar al implementar que abrir con `component: null` no lanza.)*
  - `FT-002-16 · "Aceptar" está deshabilitado mientras el id no es válido`: abrir editando un componente; poner el `input` de id (`modal().querySelector('.modal__field input[type=text]')`) a `''` y disparar `new Event('input', { bubbles: true })`; `modal().querySelector('.btn-accept').disabled` es `true`. Poner un id válido y disparar `input`; `.disabled` pasa a `false`.
  - `FT-002-17 · el borrado desde la modal pide confirmación`: espiar `window.confirm` (guardar el original, sustituir por `() => { calls++; return false; }`), abrir editando, `modal().querySelector('.btn-eliminar').click()`; `calls === 1` y, al devolver `false`, `onDelete` NO se invocó y la modal sigue abierta. Restaurar `window.confirm` en `afterEach`.
  - `FT-002-18 · validación de id: vacío, sólo espacios y duplicado de otro`: replicar la lógica privada `validateId` como en `component-crud.test.js` (`isIdValid(raw, editingId='') => raw.trim() !== '' && !getComponents().some((c) => c.id === raw.trim() && c.id !== editingId)`). Con dos componentes `alpha` y `beta` en estado: `isIdValid('')` y `isIdValid('   ')` son `false`; `isIdValid('beta', 'alpha')` es `false`; `isIdValid('beta', 'beta')` es `true` (conservar el propio); `isIdValid('gamma', 'alpha')` es `true`. *(Este caso es nivel estado; no abre modal. Complementa FT-002-16, que valida el efecto sobre el botón.)*
- [x] **`src/test/functional/component-transform.test.js` — nuevo, funcionalidad 015 (nivel state + ui).** `import` de `harness.js`; `import { resetState, mountEditMode, mountPlayMode } from '../helpers.js'`; `import { getComponents, addComponent, replaceComponent } from '../../core/state.js'`; `import { createComponent, updateComponent } from '../../core/component.js'`; `import { createDefaultComponent } from '../../ui/componentModal.js'`. `registerFeature({ primary: 15 })`. `beforeEach(resetState)`. **Ojo**: `modes/edit/editMode.js` `selectedComponentIds` es estado de módulo NO reseteado por `resetState()` — cada caso que dependa de selección usa ids de componente distintos (como `edit-context-menu.test.js`). Selectores raíz: `.carta`, `.dice`, `.board`. Casos:
  - `FT-015-01 · un componente nuevo nace con posición por defecto`: `createComponent({ type: 'carta' })` → `x` y `y` valen `100` (o el default que fije `core/component.js` — leer el valor real al implementar y asertarlo explícito). *(Si `createComponent` no fija `x`/`y` y los pone `null`/`undefined`, asertar eso y comprobar que `getComponentsBounds` los trata como `100` — ajustar el caso a lo que diga el código.)*
  - `FT-015-02 · mover un componente actualiza su posición en el estado`: `addComponent(createDefaultComponent('carta'))`; `replaceComponent(id, updateComponent(getComponents()[0], { x: 250, y: 300 }))`; `getComponents()[0].x === 250 && .y === 300`.
  - `FT-015-03 · el tirador de redimensionado sólo aparece con selección de tamaño 1`: `addComponent` de una carta; `mountEditMode()`; seleccionarla con un `click` sobre `.carta` (`content.querySelector('.carta').dispatchEvent(new MouseEvent('click', { bubbles: true }))`); re-render (`mountEditMode()` otra vez); `content.querySelector('.carta .resize-handle')` existe.
  - `FT-015-04 · con selección múltiple no hay tirador de redimensionado`: dos cartas con ids distintos; `mountEditMode()`; `click` en la primera, luego `click` con `ctrlKey: true` en la segunda (`new MouseEvent('click', { bubbles: true, ctrlKey: true })`) para selección aditiva; re-render; ninguna `.carta` tiene `.resize-handle` (`content.querySelectorAll('.carta .resize-handle').length === 0`).
  - `FT-015-05 · la clase <tipo>--movable depende del bloqueo y del modo`: carta con `bloqueado: 'ninguno'` → en `mountPlayMode()` la `.carta` tiene `.carta--movable`; con `bloqueado: 'todos'` → en `mountEditMode()` la `.carta` NO tiene `.carta--movable`; con `bloqueado: 'juego'` → en `mountEditMode()` SÍ tiene `.carta--movable` (edición permite arrastrar) y en `mountPlayMode()` NO.
  - `FT-015-06 · la insignia de candado se pinta en edición si hay bloqueo`: carta con `bloqueado: 'juego'` → en `mountEditMode()` la `.carta` tiene un `.component-lock-badge`; con `bloqueado: 'ninguno'` → no lo tiene; en `mountPlayMode()` nunca (`content.querySelector('.carta .component-lock-badge')` es `null` aunque `bloqueado !== 'ninguno'`).
  - `FT-015-07 · mover con flechas afecta a la selección pero no a los bloqueados en "todos"`: `import { moveSelectedComponent } from '../../modes/edit/editMode.js'`. Dos cartas ids distintos, una con `bloqueado: 'todos'`; `mountEditMode()`; seleccionar ambas (click + ctrl+click); `moveSelectedComponent(10, 0)`; la libre cambió su `x` en `+10`, la de `bloqueado: 'todos'` conserva su `x`. *(Verificar al implementar el nombre y firma exactos de `moveSelectedComponent` — la doc dice `(dx, dy)`, paso 1px; si internamente depende de `getState().mode`, `mountEditMode()` ya deja el modo en EDIT.)*
- [x] **`src/test/functional/error-modal.test.js` — nuevo, funcionalidad 033 (nivel ui).** `import` de `harness.js` (`describe, it, expect, afterEach, registerFeature`); `import { showErrorModal } from '../../ui/errorModal.js'`; `import { initI18n } from '../../core/i18n.js'` y llamarlo una vez (`showErrorModal` usa `t('common.close')`) — o reutilizar `resetState` de `helpers.js` que no monta i18n; mejor un `beforeEach` propio `initI18n()` (idempotente). `afterEach(() => document.querySelectorAll('.modal-overlay').forEach((o) => o.remove()))`. `registerFeature({ primary: 33 })`. Helper `const errModal = () => document.body.querySelector('.modal-overlay .modal__header--error')`. Casos:
  - `FT-033-01 · showErrorModal pinta la ventana modal de error con cabecera e icono`: `showErrorModal('Título', 'Mensaje')`; `document.body.querySelector('.modal-overlay')` existe; tiene `.modal__header.modal__header--error`; dentro, `.modal__error-icon` con `textContent === '!'`; el `h2.modal__header-title` tiene `textContent === 'Título'`; el `.modal__content p` tiene `textContent === 'Mensaje'`.
  - `FT-033-02 · el bloque de detalle sólo aparece si se pasa detail`: `showErrorModal('T', 'M')` → `.modal__error-detail` es `null`. Limpiar. `showErrorModal('T', 'M', 'stack trace...')` → `.modal__error-detail` existe y su `textContent === 'stack trace...'`.
  - `FT-033-03 · el botón "Cerrar" cierra la ventana`: `showErrorModal('T', 'M')`; `document.body.querySelector('.modal-overlay .modal__footer .btn-cancel').click()`; `document.body.querySelector('.modal-overlay')` es `null`.
  - `FT-033-04 · pulsar fuera de la ventana la cierra`: `showErrorModal('T', 'M')`; sobre el `.modal-overlay`, `dispatchEvent(new MouseEvent('mousedown', { bubbles: true }))` con `target` = overlay y luego `dispatchEvent(new MouseEvent('click', { bubbles: true }))` sobre el overlay (el módulo exige `mousedown` y `click` ambos sobre el overlay); `.modal-overlay` desaparece. *(Para que `e.target === overlay`, disparar los eventos directamente sobre el nodo overlay, no sobre un hijo.)*
  - `FT-033-05 · un click dentro de la ventana NO la cierra`: `showErrorModal('T', 'M')`; `mousedown` + `click` sobre el `.modal` interior (hijo del overlay); `.modal-overlay` sigue presente.
  - `FT-033-06 · los errores usan modal y no toast`: `import { showToast } from '../../ui/toast.js'` sólo para contraste conceptual — no se puede espiar fácil; en su lugar, asertar que tras `showErrorModal(...)` NO hay ningún `.toast` en `document.body` y SÍ hay `.modal-overlay .modal__header--error`. *(Caso ligero; documenta el contrato "error ⇒ modal".)*
- [x] **`src/test/functional/component-title.test.js` — nuevo, funcionalidad 035 (nivel state + ui).** `import` de `harness.js`; `import { resetState, mountEditMode, mountPlayMode } from '../helpers.js'`; `import { getComponents, addComponent } from '../../core/state.js'`; `import { createComponent } from '../../core/component.js'`; `import { createDefaultComponent } from '../../ui/componentModal.js'`; `import { openComponentTitleModal } from '../../ui/componentTitleModal.js'`. `registerFeature({ primary: 35 })`. `beforeEach(resetState)`; `afterEach(() => document.querySelectorAll('.modal-overlay').forEach((o) => o.remove()))`. Casos:
  - `FT-035-01 · defaults del modelo de título`: `const c = createComponent({ type: 'carta' })`; `c.mostrarTitulo === false`; `c.tituloTexto` es `''` o `undefined` (asertar el valor real que fije `core/component.js`); si define colores, `c.tituloColorTexto` / `c.tituloColorFondo` / `c.tituloFondoTransparencia` con sus defaults (`'#000000'` / `'#ffffff'` / `0`). *(Leer `core/component.js` al implementar: puede que estos campos sólo existan cuando se editan; en ese caso asertar `undefined` y que el render los trata como default.)*
  - `FT-035-02 · en modo juego se pinta la etiqueta sólo si mostrarTitulo y hay texto`: carta con `mostrarTitulo: true`, `tituloTexto: 'Mi carta'` → `mountPlayMode().querySelector('.carta .component-title-label')` existe y su texto contiene `Mi carta`. Con `mostrarTitulo: true` y `tituloTexto: ''` → `.component-title-label` es `null` (no pinta caja vacía). Con `mostrarTitulo: false` y `tituloTexto: 'X'` → `null`.
  - `FT-035-03 · la etiqueta de título nunca se pinta en modo edición`: carta con `mostrarTitulo: true`, `tituloTexto: 'Visible en juego'`; `mountEditMode().querySelector('.carta .component-title-label')` es `null`.
  - `FT-035-04 · mostrarTitulo es override de grupo`: `import { ... }` lo necesario para crear un grupo (revisar `core/group.js` / `core/state.js` `loadGroups`/`addGroup`). Dos cartas en un grupo con el grupo a `mostrarTitulo: true` y las cartas a `mostrarTitulo: false` pero con `tituloTexto` no vacío → en `mountPlayMode()` ambas `.carta` muestran `.component-title-label`. Grupo a `mostrarTitulo: false` → ninguna. *(Si montar un grupo desde el test resulta desproporcionado, degradar este caso a comprobar `getEffectiveGeneralProps` directamente — revisar su ubicación en `core/group.js`; anotarlo al implementar.)*
  - `FT-035-05 · el editor de título sincroniza slider y campo, acota 0–100 y descarta NaN`: `openComponentTitleModal({ titulo: { texto: 'x', colorTexto: '#000000', colorFondo: '#ffffff', fondoTransparencia: 20 }, onAccept: spy })`. Localizar `const modal = document.body.querySelector('.modal-overlay .modal')`. El `input[type=range]` y el `input[type=text]` de transparencia: mover el range a `70` + `dispatchEvent(new Event('input'))` → el text pasa a `70`. Poner el text a `150` + `dispatchEvent(new Event('change'))` → queda `100` y el range también. Poner el text a `abc` + `change` → vuelve al último válido (`100`). Pulsar `.btn-accept` → `spy` recibe `{ texto, colorTexto, colorFondo, fondoTransparencia: 100 }`.
  - `FT-035-06 · cancelar el editor de título no invoca onAccept`: `openComponentTitleModal({ titulo: {...}, onAccept: spy })`; `.btn-cancel` de la modal → `spy` no llamado; la `.modal-overlay` desaparece.
- [x] **`src/test/functional/version-indicator.test.js` — nuevo, funcionalidad 037 (nivel state + 1 caso ui).** `import` de `harness.js` (`describe, it, expect, beforeEach, afterEach, registerFeature`); `import { resetState } from '../helpers.js'`; `import { getTableText, setTableText } from '../../core/state.js'`; `import { formatVersion, getVersionedProductName, getFullAppTitle } from '../../core/appTitle.js'`; `import { CURRENT_VERSION } from '../../data/version.js'`; `import { initI18n, t } from '../../core/i18n.js'`. `registerFeature({ primary: 37 })`. `beforeEach(() => { resetState(); initI18n(); })`. **Replicar `renderAppVersion` mínimo** en el fichero (copia fiel de `main.js#renderAppVersion`, sólo la parte de estructura DOM):
  ```js
  function renderAppVersionInto(el) {
    el.textContent = '';
    const nameLine = document.createElement('div');
    nameLine.className = 'app-version__name';
    nameLine.textContent = `BG Factory ${CURRENT_VERSION}`;
    const repoLine = document.createElement('div');
    repoLine.className = 'app-version__repo';
    const repoLink = document.createElement('a');
    repoLink.href = 'https://github.com/yeyopepe/bgfactory';
    repoLink.target = '_blank';
    repoLink.rel = 'noopener';
    repoLink.textContent = t('appVersion.repoLink');
    repoLine.appendChild(repoLink);
    const tableText = getTableText();
    if (tableText.trim() !== '') {
      const noteLine = document.createElement('div');
      noteLine.className = 'app-version__table-text';
      noteLine.textContent = tableText;
      const separator = document.createElement('hr');
      separator.className = 'app-version__separator';
      el.append(noteLine, separator, nameLine, repoLine);
      return;
    }
    el.append(nameLine, repoLine);
  }
  ```
  Casos:
  - `FT-037-01 · formatVersion() y getVersionedProductName() derivan de CURRENT_VERSION`: `formatVersion() === 'v.' + CURRENT_VERSION.slice(1)`; `getVersionedProductName() === 'BG Factory ' + formatVersion()`; `getVersionedProductName()` no depende del título editable (no llamar a `getFullAppTitle` aquí).
  - `FT-037-02 · el bloque muestra nombre+versión y el enlace a GitHub abriendo en pestaña nueva`: `const el = document.getElementById('app-version'); renderAppVersionInto(el);` `el.querySelector('.app-version__name').textContent` contiene `CURRENT_VERSION`; `el.querySelector('.app-version__repo a')` tiene `href === 'https://github.com/yeyopepe/bgfactory'`, `target === '_blank'`, `rel === 'noopener'`, y `textContent === t('appVersion.repoLink')`.
  - `FT-037-03 · sin texto libre, sólo las dos líneas fijas y sin hr`: `setTableText('')` (o dejar el default de `resetState`); `renderAppVersionInto(el)`; `el.querySelector('.app-version__table-text')` es `null`; `el.querySelector('.app-version__separator')` es `null`; `el.children.length === 2`.
  - `FT-037-04 · con texto libre, aparece por encima de las fijas y separado por hr`: `setTableText('nota de la mesa')`; `renderAppVersionInto(el)`; `el.children[0]` tiene clase `app-version__table-text` y `textContent === 'nota de la mesa'`; `el.children[1]` es un `HR` con clase `app-version__separator`; `el.children[2]` es `.app-version__name`. `el.querySelectorAll('*').length` acorde (4 nodos directos: note, hr, name, repo).
  - `FT-037-05 · el texto libre se pinta como texto plano, no como HTML`: `setTableText('<b>hola</b>\nsegunda línea')`; `renderAppVersionInto(el)`; `el.querySelector('.app-version__table-text').querySelector('b')` es `null` (no se interpretó el HTML); `.textContent` contiene el literal `<b>hola</b>`.
  - `FT-037-06 · getTableText/setTableText: default vacío y round-trip`: `resetState()` → `getTableText() === ''`; `setTableText('abc')` → `getTableText() === 'abc'`.
- [x] **Ejecutar `npm test` y comprobar el resultado.** Debe salir `exit 0`: todos los casos en verde y `TRACEABILITY.md` regenerado sin anomalías. Si `playwright` no está instalado (`exit 2`), ejecutar `npm run test:setup` una vez y reintentar. Ningún `primary` nuevo (1, 15, 33, 35, 37; el 2 ya existía) puede dar "funcionalidad inexistente" — los cinco están en `previo-sdd/design/docs/features/INDEX.md`, así que no debería haber anomalía; si la hubiera, revisar el número pasado a `registerFeature`.
- [x] **Revisar `src/test/TRACEABILITY.md` tras `npm test`.** Las funcionalidades 001, 015, 033, 035 y 037 deben salir de la sección "Funcionalidades sin ningún test" y aparecer con sus códigos `FT-*` en la tabla; la 002 debe listar ahora también los `FT-002-13..18`. El fichero lo regenera `npm test` — no editarlo a mano; sólo se versiona el resultado.

## (c) Architecture changes

`previo-sdd/design/docs/architecture/011-functional-test-framework.md` — sección "## Test files": añadir las seis filas nuevas a la tabla `| File | Feature | Level |` (`functional/infinite-table.test.js` → 001 → ui; `functional/component-modal-tabs.test.js` → 002 → state + ui; `functional/component-transform.test.js` → 015 → state + ui; `functional/error-modal.test.js` → 033 → ui; `functional/component-title.test.js` → 035 → state + ui; `functional/version-indicator.test.js` → 037 → state + ui). Nota ya presente en esa sección: "not a live inventory" — aun así el resto de ficheros están listados, así que se añaden por coherencia. No hace falta tocar nada más de ese documento (el contrato del framework no cambia).

`previo-sdd/design/docs/architecture/005-modes.md` (líneas ~10-12) — **inconsistencia detectada con el código, a corregir de paso**: describe el layout de `#mode-switcher` en modo juego como `Importar | Entrar en modo edición` + botón flotante `.mode-switcher__fit-btn`, y el botón de modo como "Entrar en modo edición" / "Salir del modo edición". El código real (`src/ui/editModeToggle.js`, `renderModeSwitcher` / `createModeButton` / `createFitButton`, reorg. 00244) y `006-ui-layer.md` dicen: modo juego → `[Importar] [Exportar] │ [Modo] [Ajustar zoom] [Configuración]` (`│` = `.toolbar-divider`); modo edición → `[Modo] [Ajustar zoom] [Configuración]`; botón de modo `createModeButton()` con texto `t('toolbar.modeEdit')` ("Modo Edición") en juego / `t('toolbar.modePlay')` ("Modo Juego") en edición, clase `mode-switcher__mode-btn`; botón "Ajustar zoom" `createFitButton('mode-switcher__fit-btn')` en `#mode-switcher` en ambos modos, handler `fitToBounds(getComponentsBounds(getComponents()))`. Actualizar esas líneas de `005-modes.md` para reflejar el estado actual (o remitir a `006-ui-layer.md` como fuente única de ese detalle). El código manda.

## (e) Verification

- [x] `npm test` termina con `exit 0` (`Total: N — OK: N — FALLOS: 0`) y la línea "Trazabilidad escrita en src/test/TRACEABILITY.md" **sin** "CON ANOMALÍAS".
- [x] En `src/test/TRACEABILITY.md`, la fila "001 — Mesa infinita con navegación pan/zoom" lista `FT-001-01 … FT-001-08`; "015 — Posición independiente…" lista `FT-015-01 … FT-015-07`; "033 — Modal de error común a toda la app" lista `FT-033-01 … FT-033-06`; "035 — Título de componente" lista `FT-035-01 … FT-035-06`; "037 — Indicador de versión y enlace al repositorio" lista `FT-037-01 … FT-037-06`. Ninguna de esas cinco aparece ya en "Funcionalidades sin ningún test".
- [x] En esa misma tabla, la fila "002 — Alta/edición/borrado de componentes con modal de tabs" incluye ahora, además de `FT-002-01 … FT-002-12`, los códigos `FT-002-13 … FT-002-18`.
- [x] La sección "Tests que declaran una funcionalidad inexistente" de `TRACEABILITY.md` sigue diciendo "_Ninguna._".
- [x] `git status` muestra como nuevos exactamente seis ficheros bajo `src/test/functional/` y como modificados `src/test/TRACEABILITY.md` y los dos documentos de arquitectura citados en (c); ningún fichero de `src/core/`, `src/ui/`, `src/modes/` ni `src/main.js` aparece modificado.
- [x] Re-ejecutar `npm test` una segunda vez da el mismo resultado (`exit 0`, sin anomalías) — descarta contaminación entre casos por estado de módulo no reseteado (`table.js` cámara, `editMode.js` `selectedComponentIds`).
