- **Creation date**: 2026-09-09

## (a) Functional notes

**Out of scope:**
- No se toca código de producción (`src/core/`, `src/ui/`, `src/modes/`). El cambio es solo tests nuevos en `src/test/functional/` más ajustes de documentación.
- Funcionalidad 31 ("Guardar a fichero"): no se crea batería de tests — no existe en el código (ver `description.md`). Solo limpieza de documentación.
- No se testea de forma exhaustiva la librería `vendor/marked.js` (es de terceros): un único caso representativo de GFM basta.
- No se comprueba maquetación real (anchuras, scroll) del Visor de documentos: el runner no carga CSS — mismo criterio acotado que las baterías existentes (`resource-image-modal.test.js`, `infinite-table.test.js`).
- No se crean fixtures nuevas: los tres ficheros construyen sus componentes al vuelo con `createDefaultComponent` / `createComponent` / `addComponent`, como `dado.test.js` y `orden-apilado.test.js`.

**Doubts resolved with the user:**
- *¿Se hace la batería de la funcionalidad 31?* No: no existe en código; en su lugar se archiva su ficha funcional y se actualiza la documentación relacionada.
- *Criterios A–H del análisis de `pv-new`:* seguir lo ya implementado en las baterías previas (un fichero por funcionalidad, nivel mixto state+ui, sin fixtures nuevas salvo necesidad, `getEffectiveGeneralProps` para overrides de grupo, `mockRandom` para deterministas, `dispatchContextMenu`/`getOpenContextMenu` para menús).

**Verificaciones hechas durante el análisis (código como fuente de verdad):**
- `src/core/component.js:195` — `syncCopyWithOriginal` **sí** propaga `accionClickDerecho` (y `interaccionesDesactivadas`, línea 194). Por tanto la ficha funcional `014-...md` es correcta y la inconsistencia está en `002-component-model.md`, cuya lista "Always propagated" de `syncCopyWithOriginal` **omite `accionClickDerecho`**. Se corrige en la sección (c).
- Identificador de tipo del Visor de documentos: `'documento'` (nombre visible "Visor de documentos"). `003-component-types.md` ya lo documenta bien.
- `createDefaultComponent` (`src/ui/componentModal.js:238`) parte de `createComponent({ type })` y luego, por tipo, sobrescribe `width`/`height`/`properties` y algunos flags. Fija `subirAlMoverInteractuar = true` para `dado` (l.251), `carta` (l.263) y `mazo` (l.268). Para `documento` (l.256-259): `width` `DEFAULT_DOCUMENTO_WIDTH = 240`, `height` `DEFAULT_DOCUMENTO_HEIGHT = 320`, `properties = { ...DEFAULT_DOCUMENTO_PROPERTIES }` = `{ tipoContenido: 'texto', contenido: '', formato: 'markdown', url: '' }`. No fija `subirAlMoverInteractuar` (queda en `false` de `createComponent`), ni `interaccionesDesactivadas` (queda `[]`), ni `accionClickDerecho` (queda `'ninguno'`).
- `createComponent()` (`src/core/component.js:6`): defaults "desnudos" — `width`/`height` `null`, `properties` `{}`, `subirAlMoverInteractuar false`, `interaccionesDesactivadas []`, `accionClickDerecho 'ninguno'`, `sincronizado true`.

## (b) Technical solution

- [x] **`src/test/functional/subir-al-interactuar.test.js` (NUEVO) — batería de la funcionalidad 013.** `registerFeature({ primary: 13 })`. `import { describe, it, expect, beforeEach, afterEach, registerFeature } from '../harness.js'` y de `../helpers.js`: `resetState, mountEditMode, mountPlayMode`. De `../../core/state.js`: `getComponents, addComponent`. De `../../core/component.js`: `createComponent`. De `../../ui/componentModal.js`: `createDefaultComponent`. Casos (prefijo `FT-013-<nn>`):
  - **FT-013-01 · valores por defecto del flag por tipo (state).** `createDefaultComponent('carta').subirAlMoverInteractuar` → `true`; ídem `'dado'`, `'mazo'`. `createDefaultComponent('texto')`, `'tableroSimple'`, `'documento'` → `false`. `createComponent({ type: 'carta' }).subirAlMoverInteractuar` → `false` (el "desnudo" no aplica el default por tipo).
  - **FT-013-02 · migración de guardado antiguo sin el campo (state).** Construir un objeto componente sin la clave `subirAlMoverInteractuar` (`const c = createComponent({ type: 'texto' }); delete c.subirAlMoverInteractuar;`), `addComponent(c)` y comprobar que se comporta como `false` — es decir, montar `mountPlayMode`, arrastrar (ver helper `dragTo` abajo) y verificar que `order` no cambia. (No hay migración explícita; ausente ≡ default `false`, ver `002-component-model.md`.)
  - **FT-013-03 · con el flag marcado, arrastrar en Modo Juego sube a `order` 1.** Sembrar 3 componentes `texto` (`t1`,`t2`,`t3`) con `subirAlMoverInteractuar: true` en `t3` (que nace en `order` 3, el de más abajo). `mountPlayMode()`. Localizar el nodo de `t3` (`.text-box`, usar marcador de contenido como en `orden-apilado.test.js` → `worldChildrenIds`) y simular arrastre: `mousedown` sobre el nodo + `mousemove` en `document` con desplazamiento + `mouseup` (patrón de arrastre real; ver helper local). Tras el drop, `getComponents().find(c=>c.id==='t3').order` → `1` y el resto recompactado (`t1`→2, `t2`→3 según su orden previo). Fuente: `modes/play/playMode.js:170` (`onMove` → `reorderComponent(component.id, 1)` si `getEffectiveGeneralProps(...).subirAlMoverInteractuar`).
  - **FT-013-04 · con el flag desmarcado, arrastrar NO reordena.** Mismo montaje pero `subirAlMoverInteractuar: false` en `t3`; tras arrastrar, `order` de `t3` sigue siendo `3` (solo cambian `x`/`y`).
  - **FT-013-05 · lanzar dado en Modo Juego sube a `order` 1.** Sembrar `dado` (`d1`, `subirAlMoverInteractuar: true`, nace en `order` 1) y dos `texto` por debajo. Reordenar `d1` a la última posición (`reorderComponent('d1', 3)` desde `core/state.js`) para que un reorden posterior sea observable. `mockRandom(new Array(64).fill(0.999))`, `mountPlayMode()`, `click` sobre el nodo `.dice`, esperar `~250 + 1000 + 400` ms (patrón de `dado.test.js` FT-020-08). Tras la tirada, `getComponents().find(c=>c.id==='d1').order` → `1`. Fuente: `playMode.js:177` (`onDiceResult`).
  - **FT-013-06 · voltear carta en Modo Juego sube a `order` 1.** Sembrar `carta` (`c1`, `subirAlMoverInteractuar: true`), reordenar a la última posición, `mountPlayMode()`, `click` sobre `.carta`, comprobar `order` → `1` (el flip es síncrono, sin timers) y `caraActual` cambiada. Fuente: `playMode.js:184` (`onCartaFlip`).
  - **FT-013-07 · sacar carta de un mazo en Modo Juego sube a `order` 1.** Sembrar `mazo` (`m1`, `subirAlMoverInteractuar: true`, ya `true` por defecto) con al menos una `carta` referenciada en `properties.cartaIds` (crear la carta como componente y añadir su id; la carta en mazo no se pinta como componente independiente). Reordenar `m1` a la última posición. `mountPlayMode()`, `click` sobre el nodo del mazo (`.carta` reutilizada para el mazo — filtrar por el que no es la reveal zone; alternativamente localizar por tamaño/última posición). Comprobar `order` de `m1` → `1`. Fuente: `playMode.js:190` (`onMazoDraw`).
  - **FT-013-08 · independencia de `bloqueado`.** `carta` con `subirAlMoverInteractuar: true` y `bloqueado: 'juego'`, reordenada a la última posición. `mountPlayMode()`: el arrastre no está disponible (`canMove` → `false`), pero `click` (voltear) sí dispara `onCartaFlip` → `order` → `1`. Comprobar además que el nodo no lleva la clase de arrastrable (`carta--movable` ausente) pero sí `carta--clickable`.
  - **FT-013-09 · exclusivo de Modo Juego: en Modo Edición arrastrar no reordena por este flag.** `texto` con `subirAlMoverInteractuar: true`, reordenado a la última posición. `mountEditMode()`, arrastrar el nodo, comprobar que `order` no cambia (en edición `onMove` no llama a `reorderComponent` por este flag — solo actualiza `x`/`y` y arrastre de grupo).
  - **FT-013-10 · override de grupo vía `getEffectiveGeneralProps`.** Importar `getEffectiveGeneralProps` de `../../core/group.js` y `loadGroups` de `../../core/state.js`. Componente `texto` con `subirAlMoverInteractuar: false` propio, dentro de un grupo cuyo valor efectivo de esa propiedad es `true` (crear el grupo con `loadGroups([...])` y el `groupId` en el componente; consultar en `group.js`/`catalogo-propiedades.test.js` la forma exacta de override de `subirAlMoverInteractuar` en un grupo — si el grupo no expone override de este campo, degradar el caso a comprobar que `getEffectiveGeneralProps` devuelve el valor propio del componente y dejar constancia en un comentario). `mountPlayMode()`, arrastrar → `order` → `1` (gana el valor efectivo del grupo). [gotcha] si al implementar se ve que `getEffectiveGeneralProps` NO expone `subirAlMoverInteractuar` como override de grupo, este caso se reduce a la verificación a nivel state (valor propio) y se anota — no se fuerza.
  - **Helper local `dragTo(node, dx, dy)`**: `node.dispatchEvent(new MouseEvent('mousedown', { bubbles:true, clientX:0, clientY:0 }))`; `document.dispatchEvent(new MouseEvent('mousemove', { bubbles:true, clientX:dx, clientY:dy }))`; `document.dispatchEvent(new MouseEvent('mouseup', { bubbles:true, clientX:dx, clientY:dy }))`. Revisar en `component-transform.test.js` / `orden-apilado.test.js` si ya existe un patrón de arrastre reutilizable y alinearse con él (nombres de eventos y sobre qué nodo). `afterEach`: `restoreAllMocks()` + limpiar `.modal-overlay`/`.context-menu` si procede.

- [x] **`src/test/functional/interacciones-programadas.test.js` (NUEVO) — batería de la funcionalidad 014.** `registerFeature({ primary: 14, secondary: [5] })` (secundaria 5 por el caso de sincronización de Copia). Imports: `harness.js` (`describe/it/expect/beforeEach/afterEach/registerFeature`); `helpers.js` (`resetState, mountEditMode, mountPlayMode, dispatchContextMenu, getOpenContextMenu, mockRandom, restoreAllMocks`); `../../core/state.js` (`getComponents, addComponent, replaceComponent`); `../../core/component.js` (`createComponent, syncCopyWithOriginal, createCopy`); `../../core/interactions.js` (`getInteractionsForType, isInteractionActive, TYPE_INTERACTIONS`); `../../ui/componentModal.js` (`createDefaultComponent, openComponentModal`). Casos (prefijo `FT-014-<nn>`):
  - **FT-014-01 · interacciones por tipo (state).** `getInteractionsForType('dado')` → array de 1 con `key: 'lanzar'`; `'carta'` → `key: 'voltear'`; `'mazo'` → `key: 'sacarCarta'`; `'texto'`, `'tableroSimple'`, `'documento'`, `'tableroPersonalizado'` → `[]`. Comprobar también `getInteractionsForType('inexistente')` → `[]`.
  - **FT-014-02 · `isInteractionActive` respeta `interaccionesDesactivadas` (state).** `isInteractionActive({ interaccionesDesactivadas: [] }, 'lanzar')` → `true`; `isInteractionActive({ interaccionesDesactivadas: ['lanzar'] }, 'lanzar')` → `false`; con el campo ausente (`{}`) → `true`.
  - **FT-014-03 · defaults del modelo (state).** `createDefaultComponent('dado').interaccionesDesactivadas` → `[]`; `.accionClickDerecho` → `'ninguno'`. `createComponent({ type: 'carta' })` → ídem. Comprobar que un componente nuevo tiene todas sus interacciones activas: `isInteractionActive(createDefaultComponent('mazo'), 'sacarCarta')` → `true`.
  - **FT-014-04 · migración de `accionClickDerecho` en guardado antiguo (state).** Importar `loadComponents` de `../../core/state.js`. `const c = createComponent({ type: 'dado' }); delete c.accionClickDerecho;` `loadComponents([c])`; `getComponents()[0].accionClickDerecho` → `'menuContextual'` (migración `migrateAccionClickDerecho`, deliberadamente distinta del default de un componente nuevo). Verificar además que un componente nuevo (con el campo presente en `'ninguno'`) NO se toca.
  - **FT-014-05 · pestaña "Interacciones" de la modal: combos y fila de click derecho (ui).** `mountEditMode()`. Crear un `dado` (`addComponent`), abrir `openComponentModal({ component: <dado>, onAccept(){} })`. En `document.querySelector('.modal-overlay .modal')`: localizar la `fieldset.modal__section` cuyo `legend.modal__section-title` sea el título de "Interacciones programadas". Comprobar: hay un `<select>` con opciones `value` `'activa'` y `'ninguna'` (interacción de click izquierdo, valor inicial `'activa'`); hay una fila de click derecho con `<select>` de opciones `value` `'ninguno'` y `'menuContextual'`, valor inicial `'ninguno'` para un componente nuevo. Cambiar el select de click izquierdo a `'ninguna'` (`value = 'ninguna'` + `dispatchEvent(new Event('change', { bubbles: true }))`) y comprobar `workingComponent`… — como `workingComponent` no es accesible, verificar el efecto vía `onAccept`: pasar un `onAccept` que capture el componente resultante, pulsar `.btn-accept`, y comprobar `interaccionesDesactivadas` contiene `'lanzar'` y `accionClickDerecho` refleja lo elegido. [gotcha] revisar en `component-modal-tabs.test.js` / `dado.test.js` (FT-020-07) el patrón exacto de apertura de modal, selector del botón aceptar y de los tabs. Para un `texto` (sin interacciones de click izquierdo), la sección se muestra igualmente y solo tiene la fila de click derecho.
  - **FT-014-06 · Modo Juego: desactivar "lanzar" impide la tirada.** `mockRandom(new Array(64).fill(0.999))`. `dado` con `interaccionesDesactivadas: ['lanzar']`, `resultadoActual: '1'`. `mountPlayMode()`. El nodo `.dice` NO lleva la clase `dice--clickable` (gating en `componentRenderer.js:1352`/`1403` por `isInteractionActive(component, 'lanzar')`). `click` sobre `.dice`, esperar el tiempo de una tirada, comprobar que `.dice__result` sigue en `'1'` y `properties.resultadoActual` no cambió. El doble-click (`onDiceOpenResult`) sí abre la modal de resultado — comprobar que `dblclick` sobre `.dice` abre `.dice-result-modal` / equivalente (ver `dado.test.js`).
  - **FT-014-07 · Modo Juego: desactivar "voltear" impide el flip.** `carta` con `interaccionesDesactivadas: ['voltear']`, `caraActual: 'trasera'`. `mountPlayMode()`. `.carta` sin clase `carta--clickable` (gating `componentRenderer.js:1871`). `click`, comprobar `caraActual` sigue `'trasera'`.
  - **FT-014-08 · Modo Juego: desactivar "sacarCarta" impide sacar del mazo.** `mazo` con `interaccionesDesactivadas: ['sacarCarta']` y `cartaIds` con una carta. `mountPlayMode()`, `click` sobre el nodo del mazo, comprobar `cartaIds` intacto (gating `componentRenderer.js:2074` por `isInteractionActive(component, 'sacarCarta')`).
  - **FT-014-09 · Modo Juego: `accionClickDerecho: 'ninguno'` → el botón derecho no hace nada.** `dado` con `accionClickDerecho: 'ninguno'`. `mountPlayMode()`, `dispatchContextMenu(<nodo .dice>)`, comprobar `getOpenContextMenu()` → `null` y que no hay componente seleccionado (`.dice` sin clase de seleccionado). Con `accionClickDerecho: 'menuContextual'` (segundo componente, otro id), `dispatchContextMenu` → `getOpenContextMenu()` no nulo. Fuente: `playMode.js:195` (`return` temprano si `accionClickDerecho === 'ninguno'`).
  - **FT-014-10 · sección informativa "Interacciones" del menú contextual refleja el ajuste.** `dado` con `accionClickDerecho: 'menuContextual'` (para poder abrir el menú) e `interaccionesDesactivadas: ['lanzar']`. `mountPlayMode()`, `dispatchContextMenu(<nodo>)`, `const menu = getOpenContextMenu()`. En `menu.querySelector('.context-menu__info')`: la fila (`.context-menu__info-row`) cuya `.context-menu__info-label` corresponde a "Clic izquierdo" tiene su `.context-menu__info-value` con la clase `context-menu__info-value--none` y el texto de "Ninguno" (`t('interaction.value.none')`). Con `interaccionesDesactivadas: []` (otro componente/caso), esa misma fila muestra el nombre de la interacción y NO lleva `--none`. Fuente: `playMode.js:72` (`getInteractionItemsFor`) + `contextMenu.js:131`.
  - **FT-014-11 · sincronización en componente tipo Copia (state, secundaria 5).** Original `dado` con `interaccionesDesactivadas: ['lanzar']` y `accionClickDerecho: 'menuContextual'`. Crear la copia con `createCopy(original, [original])` (o el camino que use `component-panel.test.js`/`synced-copies.test.js`), aplicar `syncCopyWithOriginal(copy, original)` y comprobar que el resultado tiene `interaccionesDesactivadas` = `['lanzar']` y `accionClickDerecho` = `'menuContextual'` (copiados del original — `core/component.js:194-195`). Cambiar el original (p. ej. reactivar `lanzar`) y re-sincronizar → la copia sigue al original.
  - `afterEach`: `restoreAllMocks()` + `document.querySelectorAll('.modal-overlay, .context-menu').forEach(o => o.remove())`.

- [x] **`src/test/functional/visor-documentos.test.js` (NUEVO) — batería de la funcionalidad 021.** `registerFeature({ primary: 21 })`. Imports: `harness.js`; `helpers.js` (`resetState, mountPlayMode, mountEditMode`); `../../core/state.js` (`getComponents, addComponent`); `../../core/component.js` (`createComponent`); `../../ui/componentModal.js` (`createDefaultComponent`); `../../core/markdown.js` (`markdownToHtml`); `../../core/sanitizeHtml.js` (`sanitizeHtml`). Casos (prefijo `FT-021-<nn>`):
  - **FT-021-01 · `markdownToHtml` — caso representativo GFM (state).** `const html = markdownToHtml('# T\n\n**b** ~~s~~\n\n| a | b |\n|---|---|\n| 1 | 2 |\n\n- [x] hecho\n- [ ] pendiente')`. Comprobar (con `expect(html).toContain(...)`): `<h1>`, `<strong>b</strong>`, `<del>s</del>` (o `<s>`), `<table>`, `<input` con `type="checkbox"` y `checked` en el primer item y sin `checked` en el segundo, y que los checkboxes llevan `disabled`. `markdownToHtml('')` → `''` o cadena vacía tras trim. No exhaustivo — un solo caso.
  - **FT-021-02 · `sanitizeHtml` — elimina `<script>` (state).** `sanitizeHtml('<p>ok</p><script>alert(1)</script>')` → contiene `<p>ok</p>`, no contiene `<script`. `sanitizeHtml('<div><script>x</script>text</div>')` → `text` preservado, `script` fuera.
  - **FT-021-03 · `sanitizeHtml` — elimina manejadores de evento inline (state).** `sanitizeHtml('<a href="#" onclick="x()">l</a>')` → sin atributo `onclick`, `<a` y `href="#"` preservados. `sanitizeHtml('<img src="x" ONERROR="y">')` → sin `onerror`/`ONERROR` (comprobación case-insensitive del resultado). `sanitizeHtml('<div onmouseover="z">d</div>')` → sin `onmouseover`.
  - **FT-021-04 · `sanitizeHtml` — neutraliza `javascript:` en `href`/`src` (state).** `sanitizeHtml('<a href="javascript:alert(1)">l</a>')` → sin atributo `href` (queda `<a>l</a>`). `sanitizeHtml('<a href="  javascript:x">l</a>')` (espacios por delante) → sin `href`. `sanitizeHtml('<img src="javascript:x">')` → sin `src`. `sanitizeHtml('<a href="https://ok">l</a>')` → `href` preservado.
  - **FT-021-05 · `sanitizeHtml` — conserva marcado y atributos legítimos (state).** `sanitizeHtml('<p class="c" id="i"><b>x</b> <em>y</em></p>')` → todo preservado (`class`, `id`, `<b>`, `<em>`).
  - **FT-021-06 · defaults del modelo (state).** `createDefaultComponent('documento')`: `width` → `240`, `height` → `320`, `properties` → `{ tipoContenido: 'texto', contenido: '', formato: 'markdown', url: '' }` (deep equal). `createComponent({ type: 'documento' })`: `width`/`height` `null`, `properties` `{}`.
  - **FT-021-07 · render rama texto/markdown (ui).** `addComponent` de un `documento` con `properties: { tipoContenido: 'texto', formato: 'markdown', contenido: '# Hola\n\n**mundo**' }`. `mountPlayMode()`. En `content.querySelector('.document-viewer')`: existe `.document-viewer__content` cuyo `innerHTML` contiene `<h1>` y `<strong>mundo</strong>` (resultado de `sanitizeHtml(markdownToHtml(...))`, `componentRenderer.js:1507`). Repetir con `mountEditMode()` — el visor se pinta igual en ambos modos.
  - **FT-021-08 · render rama texto/html con sanitizado (ui).** `documento` con `properties: { tipoContenido: 'texto', formato: 'html', contenido: '<p>hola</p><script>alert(1)</script><a href="javascript:x">l</a>' }`. `mountPlayMode()`. `.document-viewer__content` contiene `<p>hola</p>`, NO contiene `<script`, y el `<a>` no tiene `href`.
  - **FT-021-09 · render rama URL: iframe sandbox + aviso de error (ui).** `documento` con `properties: { tipoContenido: 'url', url: 'https://ejemplo.invalido/x' }`. `mountPlayMode()`. En `.document-viewer`: hay un `<iframe>` con atributo `sandbox` = `'allow-scripts allow-same-origin allow-popups'` y `src` = la URL. Hay un `.document-viewer__error` con el texto de `t('documentViewer.loadError')` y estilo inicial oculto (`style.display === 'none'`). Disparar `iframe.dispatchEvent(new Event('error'))` → `.document-viewer__error` pasa a visible (`style.display === 'flex'`). [Opcional, si el test tolera esperar 3s] comprobar el camino del timeout `DOCUMENTO_IFRAME_LOAD_TIMEOUT_MS` — probablemente omitir por coste; el evento `error` ya cubre la ruta del aviso.
  - **FT-021-10 · componente sin contenido = hoja en blanco sin aviso (ui).** `documento` con `properties: { tipoContenido: 'texto', contenido: '', formato: 'markdown' }` (o `createDefaultComponent('documento')`). `mountPlayMode()`. Hay `.document-viewer` y `.document-viewer__content` vacío (`innerHTML` vacío o solo espacios), y NO hay `.document-viewer__error`.
  - **FT-021-11 · conmutar `tipoContenido` no pierde la config de la otra opción (state).** Componente `documento` con `properties: { tipoContenido: 'texto', contenido: 'texto guardado', formato: 'html', url: 'https://guardada' }`. Simular el guardado desde la modal no es necesario: basta comprobar el contrato del modelo — que ambos bloques (`contenido`/`formato` y `url`) coexisten en `properties` independientemente de `tipoContenido`. Cambiar `tipoContenido` a `'url'` (objeto plano) y verificar que `contenido` y `url` siguen presentes. (Si `component-modal-tabs.test.js` o similar tiene un patrón para ejercitar los sub-render de la pestaña "Específicas" del `documento`, alinearse; si no, dejar este caso a nivel modelo con un comentario.)
  - `afterEach`: limpiar `.modal-overlay` si algún caso abre modal.

- [x] **`previo-sdd/design/docs/architecture/011-functional-test-framework.md` — registrar las 3 baterías nuevas.** En la tabla de la sección `## Test files` (al final del documento), añadir tres filas, respetando el formato de las existentes:
  - `| `functional/subir-al-interactuar.test.js` | 013 | state + ui — arrastre/tirada/flip/robo en `mountPlayMode` disparan `reorderComponent(id,1)` si `subirAlMoverInteractuar`; casos de default por tipo, independencia de `bloqueado`, exclusividad de modo juego, override de grupo. [gotcha] el runner no carga CSS: el apilado se comprueba por `order` del modelo, no por z-index |`
  - `| `functional/interacciones-programadas.test.js` | 014 (primary), 005 (secondary) | state + ui — `core/interactions.js` (`getInteractionsForType`/`isInteractionActive`); pestaña "Interacciones" de `openComponentModal` (`<select>` `activa`/`ninguna` + fila click derecho `ninguno`/`menuContextual`); efecto en modo juego (sin `dice--clickable`/`carta--clickable`, `onContextMenu` no abre menú); sección `.context-menu__info` refleja "Ninguno" (`--none`); `syncCopyWithOriginal` propaga `interaccionesDesactivadas`+`accionClickDerecho` |`
  - `| `functional/visor-documentos.test.js` | 021 | state + ui — `core/markdown.js` (marked v18.0.6, un caso GFM) y `core/sanitizeHtml.js` (quita `<script>`, `on*`, `javascript:`); render rama texto (`.document-viewer__content` = `sanitizeHtml(markdownToHtml(...))`) y rama URL (`<iframe sandbox>`, `.document-viewer__error` al evento `error`); hoja en blanco sin aviso |`
  - Si el documento tiene alguna lista de "funcionalidades sin test", quitar 013/014/021 de ella (y también 031, ver más abajo).

- [x] **`previo-sdd/design/docs/features/031-guardar-a-fichero.md` — archivar/retirar la ficha.** La funcionalidad no existe en el código (`core/fileExport.js` solo expone `downloadJson`; `ui/editModeToggle.js` solo monta Importar/Exportar JSON; confirmado por `007-persistence-build.md` §"File save"). Acción: eliminar el fichero `031-guardar-a-fichero.md`. [gotcha] `pv-do` debe seguir el criterio del proyecto para "retirar una ficha de funcionalidad": si `pv-internal-doc-files`/`INDEX.md` contempla archivar en vez de borrar, aplicar eso; en su defecto, borrar el fichero y su entrada del índice. No renumerar el resto de fichas (los números son identificadores estables).

- [x] **`previo-sdd/design/docs/features/INDEX.md` — quitar la entrada de la 031.** Eliminar la línea `- [031 — Guardar a fichero](031-guardar-a-fichero.md)` de su sección ("Persistencia y guardado" o la que corresponda). Regenerar el índice con la herramienta del framework si `pv-do` la usa (`pv-internal-doc-files`), en vez de editar a mano, cuando esté disponible.

- [x] **Ejecutar la batería y regenerar trazabilidad.** `npm test` desde la raíz del repo (requiere `npm run test:setup` una vez si Playwright/Chromium no están). Debe terminar en `exit 0`: todos los tests OK y sin anomalías de trazabilidad. `src/test/TRACEABILITY.md` se regenera solo — **no editarlo a mano**; el diff esperado es: 013, 014 y 021 pasan de `—` a tener sus `FT-*`, y (si estaban) salen de la lista "Funcionalidades sin ningún test"; la 031 sigue apareciendo como "sin test" hasta que su ficha se retire — al retirarla, `traceability.js` ya no la conoce (parsea `features/INDEX.md`), así que dejará de listarse. Verificar que no aparece la anomalía "Tests que declaran una funcionalidad inexistente".

## (c) Architecture changes

- **`previo-sdd/design/docs/architecture/011-functional-test-framework.md`**: añadir las 3 filas nuevas a la tabla `## Test files` (detallado en la sección (b)). Es el único doc de arquitectura que inventaria los ficheros de test.
- **`previo-sdd/design/docs/architecture/002-component-model.md`**: en la sección "Linked copies (`copyOf`)", lista "Always propagated" de `syncCopyWithOriginal`, **añadir `accionClickDerecho`** junto a `interaccionesDesactivadas` (ya presente). Verificado en `src/core/component.js:195`: la copia sí sincroniza `accionClickDerecho` con el original. Corrige la inconsistencia doc-vs-código detectada (la ficha funcional `014-...md` ya lo afirmaba correctamente).

## (e) Verification

- [x] `npm test` termina con `exit 0`, sin fallos y sin anomalías de trazabilidad, con los 3 ficheros nuevos ejecutándose (aparecen en el resumen `Total: N`).
- [x] `src/test/TRACEABILITY.md` (regenerado por `npm test`, no a mano) muestra códigos `FT-013-*`, `FT-014-*` y `FT-021-*` en las filas de las funcionalidades 13, 14 y 21 respectivamente; la fila 14 lista además los `FT-014-*` como secundaria de la 5.
- [x] `src/test/TRACEABILITY.md` ya no lista 013, 014, 021 ni 031 en "Funcionalidades sin ningún test" (031 desaparece porque su ficha se ha retirado y `traceability.js` deja de conocerla).
- [x] `src/test/TRACEABILITY.md` no tiene ninguna entrada en "Tests que declaran una funcionalidad inexistente".
- [x] No hay ningún cambio en `git diff` bajo `src/core/`, `src/ui/`, `src/modes/` (el cambio no toca producción).
- [x] `previo-sdd/design/docs/features/031-guardar-a-fichero.md` ya no existe (o está archivado según el criterio del proyecto) y no está referenciado en `previo-sdd/design/docs/features/INDEX.md`.
- [x] `previo-sdd/design/docs/architecture/011-functional-test-framework.md` tiene las 3 filas nuevas en `## Test files` y `previo-sdd/design/docs/architecture/002-component-model.md` incluye `accionClickDerecho` en la lista "Always propagated" de `syncCopyWithOriginal`.
- [x] Abrir manualmente (opcional, si hay dudas de un caso) el runner `src/test/runner-page.html?file=functional/visor-documentos.test.js` con un servidor estático y ver todos los `it` en verde. (Omitido: `npm test` ya ejecuta y verifica en verde los 11 casos de este fichero de forma automatizada; no hay dudas pendientes sobre ningún caso.)
