# Prompt history — 00275

Historical information about the analysis process, not current information. Records, verbatim and without rephrasing, the successive prompts with which the user raised and expanded this entry — they can be incomplete or contradictory with each other, since they reflect how the request evolved session by session, not the final result (that lives in `description.md`).

**Exclusive use of `pv-new` and `pv-fix`.** No other skill in the framework (`pv-how`, `pv-do`, `pv-status`, etc.) should read this file or take it into account: the source of truth for what's being asked is always `description.md`.

## 2026-09-15 — initial session

cuando se seleccione un componente pre-definido para insertar en la mesa debe aparecer una ventana de confirmación que permita aceptar o cancelar dónde se le informe al usuario cuántos componentes se van a crear y cómo. En el caso de la baraja francesa hay que informar que se añaden X recursos nuevos, Z componentes cartas, 1 mazo, etc...

Además, debería ofrecer la posibilidad de escribir los ids principales, en el caso de la baraja francesa debería permitir modificar el id del mazo y el prefijo del id de las cartas.

## 2026-09-15 — extension

en la lista de lo que se va a crear deberíamos especificar que de las 54 cartas, son 52 + 2 jokers

## 2026-09-15 — extension

actualiza en el mockup el id del mazo por el real por defecto que crea la aplicación
