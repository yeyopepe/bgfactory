- **Creation date**: 2026-09-15

## Functional notes

**Out of scope:** no se toca ningún otro conjunto pre-definido futuro (hoy solo existe `frenchDeck54`); el mecanismo se diseña genérico pero no se crea ningún preset nuevo. Tampoco se modifica la lista de tipos normales del modal "Añadir componente" ni su botón "Aceptar"/flujo de tipo ordinario.

**Doubts resolved with the user:** ninguna duda técnica adicional — `description.md` ya deja resueltos los puntos de análisis funcionales (comportamiento de Cancelar/Aceptar, ausencia de validación en vivo, estilo informativo sin icono de advertencia).

## (b) Technical solution

- [ ] **`src/core/presets/frenchDeck.js` — exponer un resumen ligero y aceptar ids configurables.** Añadir una función exportada `getFrenchDeckPresetSummary()` que derive el resumen a partir de `buildFrenchDeckCatalog()` sin generar SVGs ni tocar el estado, devolviendo la forma genérica que consumirá el nuevo modal (ver tarea siguiente): `{ title, defaultDeckId, defaultCardPrefix, counts: [{ icon, count, label }] }` — `title` = `t('componentTypeModal.preset.frenchDeck')`, `defaultDeckId` = `t('componentTypeModal.preset.frenchDeck.deckName')`, `defaultCardPrefix` = `'card-'` (el prefijo fijo que hoy usa `cardId()`), y `counts` con las 3 filas ya fijadas en `description.md` (54 recursos nuevos, 54 componentes carta "52 cartas + 2 comodines", 1 componente mazo) usando `catalog.length` para los dos conteos de 54 en vez de hardcodear el número. Modificar `createFrenchDeckPreset(...)` para que acepte un parámetro `{ deckId, cardPrefix } = {}` (ambos opcionales, con los mismos valores por defecto que hoy si se omiten) y lo use: el id del mazo pasa a ser `freeId(deckId ?? defaultDeckId, ...)`, y `cardId()` (`data/cardTemplates.js`) debe poder generar el id de cada carta con el prefijo indicado en vez del fijo `'card-'`.
- [ ] **`src/data/cardTemplates.js` — hacer el prefijo de `cardId()` configurable.** Añadir un parámetro opcional `prefix = 'card-'` a `cardId({ suitId, rank, jokerIndex, prefix })`, sustituyendo el literal `'card-'` de sus dos ramas (`card-joker`/`card-joker-<n>` y `card-<suitId>-<rank>`) por `` `${prefix}...` ``. `buildFrenchDeckCatalog()` sigue llamándolo sin `prefix` (usa el valor por defecto) para no romper su propio uso interno de conteo/posicionamiento; es `createFrenchDeckPreset()` quien, al construir cada `carta.id`, vuelve a derivar el id con el prefijo elegido por el usuario en vez de tomar `entry.cardId` tal cual del catálogo (recalcular con el mismo `suitId`/`rank`/`jokerIndex` que ya lleva cada `entry`, pasando el `cardPrefix` recibido).
- [ ] **`src/ui/presetConfirmModal.js` (nuevo) — modal de confirmación genérico.** Siguiendo el patrón de `ui/bulkDeleteConfirmModal.js` (`.modal-overlay`/`.modal`, header, `.modal__content`, footer `.btn-cancel`/`.btn-accept`) y el mockup aprobado `design_confirmacion-conjunto-predefinido.html` de este cambio (solo como referencia visual, no de markup/clases literal): `openPresetConfirmModal({ summary, onAccept, onCancel })`, donde `summary` es el objeto devuelto por `getFrenchDeckPresetSummary()` (`{ title, defaultDeckId, defaultCardPrefix, counts }`) — genérico para cualquier preset futuro, no acoplado a la baraja francesa. Estructura: header con `t('presetConfirmModal.title', { name: summary.title })`; `.modal__content` con un párrafo `t('presetConfirmModal.summaryLabel')`, una lista `.preset-confirm-modal__summary` de `.preset-confirm-modal__summary-item` (uno por cada entrada de `summary.counts`, con icono decorativo `aria-hidden`, `.preset-confirm-modal__summary-count` en negrita + el texto de la fila), un párrafo `t('presetConfirmModal.idsHint')`, y un `<fieldset class="modal__section">` con dos `.modal__field` (inputs de texto para id del mazo y prefijo de cartas, ambos precargados con `summary.defaultDeckId`/`summary.defaultCardPrefix`); footer con `.btn-cancel` (llama a `onCancel` si existe y cierra solo este overlay) y `.btn-accept` (llama a `onAccept({ deckId, cardPrefix })` con los valores actuales de los dos inputs, recortados con `.trim()`; si algún campo queda vacío tras el trim, usa el valor por defecto correspondiente en su lugar — no bloquea la confirmación por campo vacío) y cierra el overlay. Mismo patrón de cierre por click fuera (`mousedown`+`click` en el overlay) que el resto de modales de la app; sin botón de cierre por ESC especial — usa el genérico ya existente si `ui/globalShortcuts.js` ya cubre `.modal-overlay` (revisar y reutilizar sin añadir lógica nueva).
- [ ] **`src/ui/componentTypeModal.js` — abrir la confirmación en vez de crear directamente.** El callback `onPresetSelected` que recibe `openComponentTypeModal` pasa a firmarse como `onPresetSelected(presetId)`, invocado igual que hoy al pulsar el item de preset — sin cambios en esta función salvo que YA NO cierra el modal "Añadir componente" al pulsar el preset (quitar `overlay.remove()` de `selectPreset`): el modal de tipo debe seguir abierto detrás mientras se decide en la ventana de confirmación (`description.md`: "Cancelar no cierra el modal de selección de tipo"). El cierre del modal de tipo al aceptar pasa a ser responsabilidad de quien orquesta ambos modales (siguiente tarea).
- [ ] **`src/modes/edit/editMode.js#openAddModal` — orquestar modal de tipo + confirmación + creación.** Sustituir el cuerpo actual de `onPresetSelected` (que llamaba directo a `runWithProgressModal(...) → createFrenchDeckPreset()`) por: al recibir `presetId === 'frenchDeck54'`, llamar a `getFrenchDeckPresetSummary()` (import nuevo) y abrir `openPresetConfirmModal({ summary, onAccept: ({ deckId, cardPrefix }) => { <cerrar el modal de tipo> ; runWithProgressModal(t('componentTypeModal.preset.frenchDeck.progress'), () => { createFrenchDeckPreset({ deckId, cardPrefix }); }); } })`. Cerrar el modal de tipo en `onAccept` implica guardar una referencia a su `overlay` (o exponer un cierre) — la forma más simple sin tocar la firma pública de `openComponentTypeModal`: capturar el `overlay` devuelto o pasado por el propio `openComponentTypeModal` (revisar si conviene que esa función devuelva un `{ close }` o el elemento overlay; si no lo expone hoy, añadir ese pequeño retorno en la misma tarea de `componentTypeModal.js` anterior, en vez de duplicar la lógica de creación/cierre del overlay aquí).
- [ ] **`src/data/i18n.es.js` / `src/data/i18n.en.js` — nuevas claves.** Añadir junto a las `componentTypeModal.preset.*` existentes: `presetConfirmModal.title` (`'Añadir "{name}"'` / `'Add "{name}"'`), `presetConfirmModal.summaryLabel` (`'Se van a crear:'` / `'This will create:'`), `presetConfirmModal.idsHint` (`'Puedes cambiar estos identificadores antes de crear el conjunto:'` / `'You can change these identifiers before creating the set:'`), `presetConfirmModal.deckIdLabel` (`'Id del mazo'` / `'Deck id'`), `presetConfirmModal.cardPrefixLabel` (`'Prefijo de id de las cartas'` / `'Card id prefix'`), y las 3 filas de resumen de la baraja francesa ya existentes (`frenchDeck.tag1`/`tag2`) se reutilizan tal cual para dos de los tres conteos — añadir solo la que falta para el tercero, `presetConfirmModal.frenchDeck.resourcesCount` (`'{count} recursos nuevos'` / `'{count} new resources'`), ya que "componentes carta" y "componente mazo" pueden derivarse reformateando `tag1`/`tag2` o, si el formato no encaja como fila de resumen, añadir sus propias claves equivalentes con `{count}` interpolado en vez de reutilizar los tags literales del picker (que no lo llevan parametrizado).
- [ ] **Tests funcionales — `src/test/functional/french-deck-preset.test.js` (actualizar).** FT-002-19/20/21 siguen validando `createFrenchDeckPreset()` a nivel `state` (sin UI): añadir la llamada con `{ deckId, cardPrefix }` explícitos para cubrir el nuevo parámetro (nuevo caso, p. ej. `FT-002-22 · createFrenchDeckPreset acepta deckId y cardPrefix personalizados y los aplica al mazo y a las cartas, desambiguando igual que con los valores por defecto`), y confirmar que omitir el parámetro (como en los 3 casos existentes) sigue produciendo el mismo id/prefijo por defecto de siempre (sin tocar sus asserts).
- [ ] **Tests funcionales — nuevo `src/test/functional/preset-confirm-modal.test.js` (nuevo, nivel ui).** Cubre el flujo completo desde `mountEditMode()`: abrir "Añadir componente", pulsar el preset de baraja francesa, comprobar que aparece la ventana de confirmación con el resumen y los dos campos precargados con los valores por defecto, y que el modal de tipo original sigue en el DOM detrás (`document.body` contiene ambos overlays apilados, o al menos el modal de tipo no se ha eliminado); casos: `FT-002-23` Cancelar cierra solo la confirmación y dejа visible el modal de tipo, sin crear ningún componente ni recurso; `FT-002-24` Aceptar con los valores por defecto cierra ambos modales y crea el conjunto igual que antes (54 cartas + 1 mazo con los ids por defecto); `FT-002-25` Aceptar con un id de mazo y un prefijo de carta distintos a los por defecto los aplica a los componentes creados; `FT-002-26` un id de mazo que coincide con uno ya existente se desambigua con sufijo `(n)` igual que hoy, sin bloquear la confirmación.

Orden de implementación: primero las funciones puras (`cardTemplates.js` → `frenchDeck.js`), luego el modal nuevo (`presetConfirmModal.js`), luego el cableado UI (`componentTypeModal.js` → `editMode.js`), luego i18n, y por último los tests (los del nivel `state` primero, los de `ui` después, ya que dependen del cableado completo).

## (d) Style changes

`previo-sdd/design/docs/style/003-modales-menus.md`, sección "Pre-defined set item (immediate action in a selection list)": actualizar la frase `runs the action and calls overlay.remove() directly` — ya no es cierto tras este cambio, el click en el item de preset abre la ventana de confirmación en vez de ejecutar la acción y cerrar el modal de tipo directamente; el modal de tipo permanece abierto hasta que se decide en la confirmación. Añadir una entrada nueva para el patrón `ui/presetConfirmModal.js` (nueva ventana de confirmación) documentando: reutiliza `.modal-overlay`/`.modal`/`.btn-cancel`/`.btn-accept` (no crea patrón visual nuevo), lista de resumen de conteos agregados (`.preset-confirm-modal__summary`, filas no accionables — mismo criterio de fila de solo lectura que `.context-menu__info-row`, sin el fondo/borde de acento reservado a `.component-type-modal__preset-item`), y un `.modal__section` con los dos campos de id editables precargados con su valor por defecto. Señalar que es el punto de entrada genérico para cualquier futuro conjunto pre-definido, no solo la baraja francesa.

## (e) Verification

- [ ] Abrir "Añadir componente" y pulsar "Baraja francesa estándar (54 cartas)": aparece la ventana de confirmación con el resumen "54 recursos nuevos", "54 componentes carta (52 cartas + 2 comodines)", "1 componente mazo", y los dos campos de id precargados con "Baraja Francesa" y "card-" respectivamente; el modal "Añadir componente" ya no se ha cerrado, sigue visible detrás.
- [ ] Pulsar "Cancelar" en la ventana de confirmación: se cierra solo esa ventana, el modal "Añadir componente" sigue abierto y no se ha creado ningún componente ni recurso nuevo en la mesa/panel de componentes.
- [ ] Pulsar "Aceptar" sin modificar los campos: se cierran ambas ventanas, se muestra el indicador de progreso brevemente, y aparecen en la mesa 54 cartas + 1 mazo con los mismos ids/comportamiento que la generación actual (sin cambios respecto a antes de este cambio).
- [ ] Cambiar el id del mazo y el prefijo de las cartas antes de pulsar "Aceptar": el componente mazo resultante usa el id introducido, y cada carta usa el prefijo introducido en vez de "card-" (manteniendo el resto del patrón `<prefijo><palo>-<valor>` / `<prefijo>joker`).
- [ ] Generar la baraja francesa una vez, y luego repetir el proceso con el mismo id de mazo (dejándolo en su valor por defecto sin cambiar): el segundo mazo se renombra automáticamente con sufijo `(1)` sin bloquear ni mostrar error, igual que ya ocurría antes de este cambio.
- [ ] Ejecutar `npm test`: todos los tests pasan, incluyendo los nuevos `FT-002-22` a `FT-002-26`, y `TRACEABILITY.md` no reporta anomalías.
