- **Creation date**: 2026-08-15
- **Risk**: [pending recalculation]

## (a) Functional notes

**Out of scope:**

- No se trocean secciones mixtas (contenido+aspecto indivisibles) para extraer solo su parte visual. Se quedan tal cual en "Específicas": `proporcionField` de `'carta'` (determina forma pero está ligado a redimensionado/contenido), los botones "Editar diseño de la carta"/"Editar diseño del tablero" (`editField`, abren el Editor visual completo — subsistema propio, no campos sueltos), la sección "Estilo de la carta" (copiar/pegar estilo — utilidad transversal, no un control visual en sí), y en `'mazo'` las secciones `revealSection` (disposición/texto/cara revelada) e `imagenSection` (imagen propia, mezcla contenido+aspecto).
- Colores/bordes definidos dentro de `caraFrontal`/`caraTrasera` (`'carta'`) o `cara` (`'tableroPersonalizado'`) no se mueven — se configuran ya dentro del Editor visual (`ui/visualEditorModal.js`), no en esta modal.
- No se añade ningún límite de profundidad distinto por tipo de componente — el mismo tope máximo (40px, ver más abajo) aplica a los 8 tipos por igual.
- No se toca `getComponentsBounds` (`ui/componentRenderer.js`): el desbordamiento visual de la extrusión se tolera sin ajuste, igual que ya ocurre hoy con el offset de profundidad de `'dado'`.

**Doubts resolved with the user:**

- ¿Técnica visual de extrusión? → Capas sólidas apiladas (`box-shadow`/`filter: drop-shadow` duro, sin blur, offsets de 1px), no una copia con sombra difusa (la técnica actual de `'dado'` "parece solo una sombra").
- ¿Se unifica `'dado'` con la propiedad general nueva? → Sí, sustituye su mecanismo propio (`renderDiceSilhouette`).
- ¿Color de la extrusión? → Configurable por separado (`colorExtrusion`), con cálculo automático (`shadeColor(colorBase, -0.25)`, mismo porcentaje que ya usa `'dado'` hoy) si no se especifica.
- ¿Extrusión en `'texto'`, que sí tiene `colorFondo` configurable? → Sin efecto en ningún caso, decisión explícita para no introducir una regla condicionada al estado de otra propiedad.
- ¿Alcance de la pestaña "Visuales"? → Agrupa Tamaño (trasladado desde "Generales") y Profundidad/Color de extrusión (nuevo), más las secciones ya existentes en "Específicas" cuyo propósito es puramente de aspecto (ver tabla de la tarea 6 más abajo), movidas sección completa a sección completa, sin trocear las mixtas.
- ¿Dónde se inserta la pestaña nueva? → Entre "Generales" y "Específicas". La pestaña "Copias" (ya existente, para componentes con copias vinculadas) no se ve afectada.
- Límite máximo de profundidad: **40px**, confirmado tras la comparativa visual en `design_extrusion.html` (sección 6) sin objeción del usuario.
- Decisión de diseño (no necesitó confirmación explícita, se documenta aquí por su efecto observable): al mover la sección "Fondo" completa de `'tableroSimple'` a "Visuales" (ver tarea 6), este tipo se queda sin ninguna propiedad no-visual en "Específicas" — es el caso real que activa el mensaje "Este objeto no tiene propiedades" con el catálogo de tipos actual.

## (b) Technical solution

- [ ] **`src/core/component.js` — añadir campos raíz `profundidad`/`colorExtrusion`.** En `createComponent(params)`: añadir `profundidad: params.profundidad ?? 0` y `colorExtrusion: params.colorExtrusion ?? null` al objeto por defecto que construye y devuelve (mismo patrón que `bloqueado`/`oculto`). `updateComponent` no necesita cambios (merge superficial genérico ya los propaga).
- [ ] **`src/core/component.js` — sincronizar en copias vinculadas.** En `syncCopyWithOriginal` (líneas ~174-201), añadir `profundidad` y `colorExtrusion` a la lista explícita de campos siempre propagados (mismo grupo que `width`/`height`/`mostrarTooltip` — configuración visual del original, no estado de juego ni posición).
- [ ] **`src/ui/componentRenderer.js` — helper compartido para construir la extrusión.** Añadir una función `buildExtrusionLayers(profundidad, color, { asFilter })` que, dado `profundidad` (número) y `color` (string), devuelva la cadena de capas apiladas:
  - Si `!asFilter`: `Array.from({length: profundidad}, (_, i) => \`${i+1}px ${i+1}px 0 0 ${color}\`).join(', ')` (para `box-shadow`).
  - Si `asFilter`: mismo bucle con `drop-shadow(${i+1}px ${i+1}px 0 ${color})` unidos por espacio (para `filter`).
  - Si `profundidad <= 0`, devuelve cadena vacía / `null` (sin capas).
- [ ] **`src/ui/componentRenderer.js` — determinar el color de extrusión por componente.** Helper `resolveExtrusionColor(component, colorBase)`: devuelve `component.colorExtrusion` si tiene valor, si no `shadeColor(colorBase, -0.25)`. `colorBase` lo calcula cada rama de tipo con su propio color de referencia (`properties.colorCuerpo` en `'dado'`, `properties.bordeColor` en `'tableroSimple'`/`'tableroPersonalizado'`, `properties.colorFondo` en `'carta'` si tiene, color de fondo por defecto si no — a definir caso a caso al implementar cada rama).
- [ ] **`src/ui/componentRenderer.js` — aplicar extrusión en tipos rectangulares (`box-shadow`).** Para `'tableroSimple'`, `'tableroPersonalizado'`, `'carta'` (proporciones rectangular/circular, sin `clip-path`), `'mazo'` (reutiliza `.carta`), `'texto'` (**excluido, no aplica nunca**), `'documento'`: si `component.profundidad > 0` (y `type !== 'texto'`), construir `element.style.boxShadow = extrusionLayers + (tieneSombraDeContacto ? ', var(--shadow-1)' : '')` — `tieneSombraDeContacto` es `true` salvo que el tipo tenga `properties.sombra === false` (`'tableroSimple'`/`'tableroPersonalizado'`, respetando su checkbox "Sombra" existente). Si `profundidad === 0`, **no tocar `style.boxShadow`** (se deja que la clase CSS existente controle la sombra de contacto, sin cambios de comportamiento).
- [ ] **`src/ui/componentRenderer.js` — aplicar extrusión en tipos no rectangulares (`filter: drop-shadow`).** Para `'carta'` con proporción hexagonal/triangular (clases `.carta--hex`/`.carta--triangle`) y `'dado'`: si `profundidad > 0`, construir `element.style.filter = extrusionLayers(asFilter=true) + ' ' + contactDropShadow`, donde `contactDropShadow` replica el valor estático ya existente en `main.css` (`.dice`: `drop-shadow(0 5px 8px rgba(0, 0, 0, 0.28))`; `.carta--hex, .carta--triangle`: `drop-shadow(0 2px 6px rgba(0,0,0,0.10)) drop-shadow(0 1px 2px rgba(0,0,0,0.08))`). Aplicar siempre al **elemento contenedor exterior** (`.dice` raíz, `.carta` raíz) — nunca al elemento hijo con `clip-path`/SVG interior, replicando el patrón ya existente hoy en el código real (filtro y recorte en elementos DOM distintos). Si `profundidad === 0`, no tocar `style.filter` (la clase CSS sigue controlando la sombra de contacto).
- [ ] **`src/ui/componentRenderer.js` — eliminar el mecanismo propio de `'dado'`.** Quitar de `renderDiceSilhouette` (líneas ~142-236) la capa de profundidad hardcodeada (`depthColor`/`depthOffset`, la llamada `addPolygon(points, depthColor, {x: depthOffset, y: depthOffset})`) — la profundidad de `'dado'` pasa a resolverse igual que el resto de tipos, vía `component.profundidad`/`component.colorExtrusion` y el `filter: drop-shadow` apilado de la tarea anterior. Fijar el valor por defecto de `'dado'` (usado por `createDefaultComponent`/`DEFAULT_DADO_PROPERTIES` en `ui/componentModal.js`) a una `profundidad` que aproxime visualmente el offset actual (`size * 0.05`, ver tarea de mockup validado) — traducir a un valor fijo en px razonable para el tamaño por defecto del dado (validar visualmente al implementar, no un cálculo proporcional al tamaño).
- [ ] **`src/ui/componentModal.js` — nueva pestaña "Visuales".** Insertar `createTab('visual', 'Visuales')` justo después de `createTab('general', 'Generales')` (línea ~299) y antes de `createTab('specific', 'Específicas')` (línea ~836), para que quede en ese orden en la UI. Guardar referencia `const visualContent = tabContents.get('visual').content;`.
- [ ] **`src/ui/componentModal.js` — mover la sección "Tamaño".** Cambiar `generalContent.appendChild(sizeSection)` (línea ~611) por `visualContent.appendChild(sizeSection)`. Sin más cambios: `sizeSection` es un nodo DOM ya construido de forma independiente, y el listener de `keepRatioCheckbox` lee `cartaProporcionSelect` por closure en tiempo de evento (se rellena en `renderCartaSpecificFields`, invocada siempre al abrir la modal) — mover el nodo a otra pestaña no lo rompe.
- [ ] **`src/ui/componentModal.js` — nuevo campo "Extrusión".** En `visualContent`, añadir un `fieldset.modal__section` con legend "Extrusión" y una fila de dos campos (mismo patrón DOM que `borderRow`/`borderColorField`/`borderWidthField` de `'tableroSimple'`, líneas ~1052-1090: `div.modal__field` exterior → `div` interior `flex; gap:0.5rem` → dos `div` `flex:1`): "Profundidad" (`input[type=number] min=0 max=40`, valor `workingComponent.profundidad ?? 0`, listener que escribe `workingComponent.profundidad = parseInt(...)` clampado a `[0, 40]`) y "Color de extrusión" (`input[type=color]`, valor `workingComponent.colorExtrusion` o un color neutro de partida si es `null`, con checkbox u opción "Automático" para volver a `null` — a decidir el control exacto al implementar, siguiendo el patrón ya usado en `borderActiveCheckbox` para alternar activo/inactivo). Este campo se añade **una sola vez**, no por tipo — es transversal a los 8 tipos, igual que "Tamaño". Para `'texto'`, dejar el campo visible pero sin efecto (más simple y consistente que ocultarlo condicionalmente; documentar mediante `createHelpIcon` que no tiene efecto en este tipo).
- [ ] **`src/ui/componentModal.js` — mover secciones visuales específicas por tipo a `visualContent`.** Por tipo, las funciones `render*SpecificFields(container)` reciben ahora también `visualContainer` (referencia a `visualContent`) y hacen `visualContainer.appendChild(...)` en vez de `container.appendChild(...)` para las secciones/campos listados:

  | Tipo | Se mueve a "Visuales" | Se queda en "Específicas" |
  |---|---|---|
  | `'texto'` | `fontSizeField`, `textColorField`, `bgColorField` (agrupados en un `fieldset` "Visual" nuevo en Visuales, no tenían fieldset propio) | `contentField` |
  | `'tableroSimple'` | `biseladoField`, `sombraField`, `borderSection` completo, `bgSection` completo (selector de tipo de fondo + botón configurar) | *(nada — activa el mensaje "Este objeto no tiene propiedades")* |
  | `'tableroPersonalizado'` | `biseladoField`, `sombraField` (fieldset "Visual" ya existente, se mueve tal cual) | `editField` ("Editar diseño del tablero") |
  | `'dado'` | `bodyColorField`, `numColorField`, `fontField` | `modeField`, `maxField`, `listField` |
  | `'documento'` | *(nada, no tiene controles visuales)* | `tipoField`, `contentField`/`formatField`, `urlField` |
  | `'carta'` | `esquinasRedondeadas` (checkbox) | `proporcionField`, `editField` ("Editar diseño de la carta"), `styleSection` |
  | `'mazo'` | `formaSection` (forma + orientación) | `revealSection`, `imagenSection` |

- [ ] **`src/ui/componentModal.js` — mensaje "sin propiedades" en "Específicas".** Al final de `renderSpecificTab()` (tras invocar el `render*SpecificFields` del tipo correspondiente), si el contenedor de "Específicas" (`specificContent`/`container`, según nombre real) quedó sin ningún hijo (`container.children.length === 0`), añadir un elemento de mensaje ("Este objeto no tiene propiedades", estilo de texto secundario centrado — reutilizar clase ya existente para estados vacíos si el proyecto tiene alguna, o una clase nueva sencilla si no). Implementarlo de forma genérica (comprobando el contenedor, no hardcodeando `type === 'tableroSimple'`), para que se active automáticamente con cualquier tipo futuro que se quede sin propiedades no-visuales.
- [ ] **`src/ui/componentModal.js` — `DEFAULT_DADO_PROPERTIES`/`createDefaultComponent`.** Ajustar el valor por defecto de `profundidad` para `'dado'` al crear un componente nuevo de este tipo (ver tarea de arriba sobre el valor que aproxima el aspecto actual).
- [ ] **Ficheros de prueba (`src/test/*.json`).** Añadir o actualizar al menos un ejemplo con `profundidad`/`colorExtrusion` configurados en algún componente, para poder probar el nuevo campo importando el fichero.

## (c) Architecture changes

- **`design/docs/architecture/01-component-model.md`**: añadir `profundidad: number` y `colorExtrusion: string | null` a la tabla de "Campos generales" (con su fila de default/uso/quién lo edita, igual que el resto), y una nota en la sección "Notas sobre migraciones silenciosas al cargar" (ausencia de campo se comporta como `0`/`null`, sin migración explícita — mismo criterio que `oculto`/`mostrarTooltip`). Añadir también `profundidad`/`colorExtrusion` a la lista de campos "siempre propagados" en la sección "Copias vinculadas (`copyOf`)".

## (d) Style changes

- **`design/docs/style/INDEX.md`**, sección "Bisel/profundidad" (§13): reescribir para reflejar que `'dado'` ya no tiene mecanismo propio de profundidad — pasa a usar la propiedad general `profundidad`/`colorExtrusion` como el resto de tipos, con la técnica de capas sólidas apiladas. Mantener la mención al bisel de dos tonos (`'tableroSimple'`/`'tableroPersonalizado'`, sin cambios) como capa independiente y compatible con la extrusión.
- **`design/docs/style/01-tokens-visual.md`**, §6 "Elevación, sombra y transición": añadir una subsección nueva ("Extrusión configurable") documentando la técnica (capas de `box-shadow`/`filter: drop-shadow` sólidas apiladas sin blur, offset 1px por capa, color automático `shadeColor(base, -0.25)` o elegido por el usuario, tope `40px`) y su relación con el sistema de elevación existente: son conceptos independientes y compatibles (elevación = sombra de contacto con el "suelo"; extrusión = grosor/cuerpo del propio componente), sin introducir un cuarto nivel de elevación.
- **`design/docs/style/03-modales-menus.md`**: si existe una sección sobre "secciones dentro de pestañas" o estructura de pestañas de esta modal, añadir una mención breve de la pestaña "Visuales" nueva como patrón transversal (agrupa Tamaño + controles de aspecto), para que quede documentada junto al resto de convenciones de pestañas.

## (e) Verification

- [ ] Al editar cualquier componente de cualquiera de los 8 tipos, la pestaña "Generales" ya no muestra la sección "Tamaño" y la nueva pestaña "Visuales" (entre "Generales" y "Específicas") sí la muestra, junto con "Profundidad"/"Color de extrusión".
- [ ] Con `profundidad = 0` (valor por defecto), el aspecto de cualquier componente existente no cambia respecto a antes del cambio.
- [ ] Al subir la "Profundidad" de un `'tableroSimple'`, `'carta'` o `'mazo'` por encima de 0, aparece un cuerpo sólido con grosor visible (no una sombra difusa), en el color automático (tono oscurecido del propio color) salvo que se haya elegido un "Color de extrusión" propio, en cuyo caso se usa ese color tal cual.
- [ ] El bisel de `'tableroSimple'`/`'tableroPersonalizado'` y la sombra de contacto de nivel 1 siguen viéndose con normalidad junto a la extrusión, sin que ninguna sustituya a la otra.
- [ ] Un `'dado'` con `profundidad` por defecto se ve con una sensación de grosor similar a la que tenía antes del cambio, pero como cuerpo sólido en vez de sombra difusa.
- [ ] Una `'carta'` con proporción hexagonal o triangular muestra la extrusión siguiendo su silueta recortada (no la caja rectangular), sin que el `clip-path` la deje invisible.
- [ ] El campo "Profundidad" no admite valores por debajo de `0` ni por encima de `40`.
- [ ] Un componente `'texto'` no muestra ningún efecto de extrusión, tenga o no tenga fondo de color configurado.
- [ ] La pestaña "Específicas" de un `'tableroSimple'` muestra "Este objeto no tiene propiedades" (todos sus controles visuales se movieron a "Visuales"). El resto de tipos siguen mostrando contenido en "Específicas".
- [ ] Al guardar, recargar la página, o exportar/importar el fichero de partida, `profundidad`/`colorExtrusion` se conservan igual que el resto de campos del componente.
- [ ] Al cambiar `profundidad`/`colorExtrusion` en un componente original con copias vinculadas y sincronizadas, las copias reflejan el cambio automáticamente (igual que ya ocurre hoy con `width`/`height`).
